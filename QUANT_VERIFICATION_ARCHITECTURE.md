# 安心看股 × 定量核实：第一期架构

## 产品边界

定量核实是股票研究中的证据层，不是一级导航、自然语言选股、预测模型或自动交易系统。它回答“过去在明确假设下发生过什么”，不回答“现在应该买还是卖”。

## 数据流

```text
自然语言问题
→ LocalQuantParser（候选结构，不执行代码）
→ 用户逐项确认
→ Historical Test Engine（确定性计算）
→ Quant Audit（样本、成本、稳定性、集中度、数据与成交限制）
→ Research evidence
→ Decision Review Adapter（转成待处理问题）
→ DecisionResult / History
```

Python 领域实现位于 `src/quant_verification/`，FastAPI 提供 `/v1/quant/parse` 与 `/v1/quant/run`。当前 Sites 前端还提供等价的服务器端接口 `/api/quant/parse` 与 `/api/quant/run`，确保独立部署时不依赖 AI Key、Python 服务或外网。

## 领域模型

- `QuantHypothesis`：原始问题、股票代码、条件、观察起点、持有期、调仓频率、基准、时间区间、样本外切分、成本、敏感范围、复权和 A 股执行假设。
- `QuantTestResult`：引擎版本、数据截止日期、样本数量、样本内/样本外指标、成本前后、回撤、最长不利阶段、换手、集中度、敏感性、警告与审慎结论。
- `QuantAudit`：未来数据、幸存者偏差、样本不足、参数脆弱、收益集中、成本敏感、成交限制与数据覆盖。

所有字段随 `DecisionResult.quantVerification` 保存。该字段可选，因此旧记录无需迁移即可继续读取。独立保存的检验结果进入现有个人云端快照 `quantVerifications`，D1 表结构不变。

## 第一阶段数据模式

当前历史检验使用固定、可复现、明确标注的演示样本，截止日期为 2025-12-31。它用于验证完整交互、计算和审计流程，不冒充实时全市场回测。算法以固定种子生成低频观察样本，同一问题和股票代码得到相同的核心结果。

## 显式假设

- 前复权日线；
- 财务条件从实际披露日后的首个可交易日开始观察，禁止使用未披露数据；
- 时间顺序样本内/样本外切分，不随机打乱；
- 佣金双边 3bp、卖出印花税 5bp、单边滑点 5bp；
- T+1、停牌、涨跌停、100 股最小单位；
- 排除 ST 与上市不足 250 日样本；
- 缺失数据不插入虚构精确值；
- 固定演示股票池无法消除幸存者偏差，因此始终显示审计警告。

## 结论降级

结果只允许“提供有限支持”“削弱当前判断”“证据不足”“无法完成检验”。样本不足、样本外失效、参数反转或收益高度集中时自动降级；任何结论都附带“历史结果不代表本次会上涨”。

## 不做

不接受或执行用户 Python，不使用 `eval`/`exec`，不自动选择股票，不输出目标价、胜率神器或买卖建议，不接券商和自动交易。生产级全市场回测、历史成分股数据库和机构级撮合引擎属于后续阶段。
