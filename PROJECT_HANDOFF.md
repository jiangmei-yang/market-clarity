# 安心看股：团队项目共享与协作说明

> 文档状态：2026-07-29 共享基线
>
> 阅读对象：首次加入项目的组员、评审者，以及接手某一模块的 Codex 会话
>
> 专项交接：[决策验证模块交接](DECISION_VALIDATION_HANDOFF.md)

## 1. 一句话说明

安心看股（Market Clarity）是一款面向 A 股普通投资者的网页版研究与交易前决策验证工作台。它帮助用户把已有信息、个人判断、持仓影响和自定规则放在一起检查，但不替用户选股、预测涨跌或执行交易。

## 2. 用户问题

目标用户通常已经能从东方财富、同花顺、雪球、新闻或社交平台获得大量信息。真正未被解决的问题是：

- 信息很多，但事实、观点、传闻和预测混在一起；
- 下单前没有系统检查信息来源、仓位、潜在损失和判断失效条件；
- 容易只看支持原判断的材料；
- 决定作出后，没有保存当时依据，之后难以复盘。

本产品的价值不是“给出更聪明的股票结论”，而是减少用户在重要决定前遗漏的检查项。

## 3. 产品定位与边界

### 产品做什么

- 整理行情、财务、公告、新闻和用户提供的说法；
- 并列展示支持证据、反方证据和待核实信息；
- 把交易理由拆成事实、未核实说法、推断和情绪/紧迫性表达；
- 依据用户确认过的个人规则计算仓位、集中度和下跌情景金额；
- 帮助用户明确失效条件和下一次复核点；
- 保存用户选择“维持、修改或稍后再看”及修改前后差异；
- 提供边界明确、由用户确认后运行的历史定量核实。

### 产品不做什么

- 不推荐具体股票；
- 不输出“应该买入”“应该卖出”或目标价；
- 不承诺收益，不把历史检验解释成未来预测；
- 不连接券商，不自动下单；
- 不把媒体报道、社交观点或关键词命中升级成已证实事实；
- 不进行心理诊断；只有明确自伤危险表达才停止股票审查并显示支持性分流；
- 当前不以小程序、Android、iOS 或完整 PWA 为正式交付目标。

产品语气应保持清醒、可信、克制。任何功能设计都应遵守“证据与推断分开、数字先于形容词、最终由用户决定”。

## 4. 核心用户流程

### 首次使用

```text
描述个人资金与提醒边界
→ 系统转成结构化规则
→ 用户逐项修改和确认
→ 保存为之后检查使用的个人规则
```

### 日常研究到决策

```text
搜索股票
→ 阅读行情、财务、事件及正反证据
→ 写下计划、金额、理由和失效条件
→ 确认系统如何拆解理由
→ 核实外部说法并显示资料覆盖
→ 确定性计算仓位、集中度和下跌情景
→ 对照个人规则
→ 用户维持、修改或延后
→ 保存记录并在约定时间复核
```

### 定量核实分支

```text
提出历史问题
→ 生成可编辑候选条件
→ 用户确认对象、期限、成本和样本切分
→ 确定性引擎运行历史检验
→ 显示样本内外、回撤、稳定性、集中度和限制
→ 作为一项研究证据带入决策验证
```

确认只表示“系统理解无误”，不表示说法真实、策略有效或值得交易。

## 5. 系统结构

产品概念上分为两层：

```text
Information Layer
行情 / 财务 / 公告 / 新闻 / 研究证据
                 ↓
Decision Layer
理由拆解 / 信息核实 / 个人规则 / 金额影响 / 用户选择 / 复盘
```

Decision Layer 是项目的核心差异化。Information Layer 可以替换数据提供方，但不能直接替用户作决定。

### 当前仓库中的两条实现线

仓库同时保留两套界面相关代码，加入项目时必须先确认任务属于哪一条线：

1. `web/`：正式桌面网页平台，包含工作台、研究、机会检查、决策、评估和 Cloudflare/Vinext 相关实现。新增桌面产品工作优先检查这一目录。
2. `app.py`、`pages/`、`static/`：较早的 Streamlit/FastAPI MVP 与独立工具页面，仍包含可复用业务流程、演示和兼容入口，不应在不确认的情况下与新版前端重复实现同一功能。

共享业务逻辑主要位于 `src/`，后端入口为 `api.py`。

## 6. 重要目录

| 路径 | 作用 |
|---|---|
| `web/app/` | 正式 Web 路由、组件、服务端逻辑与 API routes |
| `web/tests/` | 正式 Web 构建、内容边界和行为测试 |
| `src/decision_review/` | 交易计划模型、理由拆解、证据检索和审查编排 |
| `src/risk_engine/` | 确定性风险计算 |
| `web/app/lib/quant-verification.ts`、`web/app/lib/quant-research.ts` | 自然语言候选条件和历史定量核实 |
| `src/services/` | 新闻、研究、股票分析等共享服务 |
| `src/data_providers/` | 真实公开数据、缓存和演示降级 |
| `web/app/lib/course-evaluation.ts` | 规则案例与模型评估定义 |
| `web/tests/` | Web 产品边界、失败控制和幻觉防护测试 |
| `tests/` | Python 业务与 API 测试 |
| `docs/`、`web/docs/` | 产品证据、设计、演进记录与验证材料 |

## 7. 当前已经具备的能力

- 自然语言个人规则，经用户确认后才保存；
- 研究页的支持证据、反方证据和待核实信息；
- 正式披露、媒体报道和市场观点分级；
- 理由拆解及无 AI Key 的规则降级；
- 单股/行业占比、单笔金额和 10%/20%/30% 下跌情景计算；
- 公告检索、来源链接、数据新鲜度和失败降级；
- 决策记录、修改前后对比和 CSV 导出；
- 定量核实的样本内外、成本、回撤、稳定性和审计警告；
- 90 秒演示及匿名用户测试流程；
- 确定性 golden/hallucination evaluation；
- 新版网页中的工作台、模型设置、评估页和数据源可靠性采样。

## 8. 当前最重要的已知限制

- 决策信息核实仍大量依赖公告标题关键词；相关标题不等于正文支持完整说法；
- 检索覆盖会受到时间窗口、上游字段、网络和限流影响；
- “未找到”只能表示当前检索范围内没有直接证据，不能表示事件不存在；
- LLM 整理不能替代来源核对，确定性规则仍是金额和边界计算的唯一依据；
- 真实外部用户的完成率、复访和付费行为证据仍需继续积累；
- 仓库包含大量历史材料与进行中的本地修改，修改前必须检查 `git status`，不得覆盖其他组员工作。

## 9. 数据、隐私与安全规则

- 不得提交 API Key、Token、密码、真实证券账户或个人金融明细；
- `.env.example` 只能放占位说明，不放真实值；
- 密钥只放服务器 Secrets 或本地被忽略的配置；
- 不在日志、截图、聊天、测试 fixture 或浏览器存储中记录密钥；
- 匿名体验数据不保存股票持仓金额、完整交易理由或直接身份信息；
- 用户提供内容不得未经确认发送给额外第三方；
- 明确区分真实、缓存、演示、过期和不可用数据；
- 自动配置、规则转换和 AI 解析必须先预览/确认，再产生持久影响。

## 10. 本地运行与验证

### Python/Streamlit 业务基线

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements-dev.txt
python -m streamlit run app.py
```

业务测试与离线评估：

```bash
pytest
```

最小 API：

```bash
uvicorn api:app --reload --port 8000
```

### 新版网页平台

需要 Node.js `>=22.13.0`：

```bash
cd web
npm install
npm run dev
```

常用验证：

```bash
npm run lint
npm run build
npm test
```

`npm test` 会先构建并运行 `web/tests/*.test.mjs`，时间明显长于单一 Node 测试。提交前按改动风险选择最小相关测试，合并前再运行完整验证。

## 11. 团队协作约定

### 开始任务前

1. 阅读本文件及任务对应的专项 handoff；
2. 运行 `git status --short`，确认哪些改动属于别人；
3. 写清楚自己的范围、非范围和验收标准；
4. 使用独立分支，避免多人同时编辑超大型页面；
5. 如需修改公共数据模型或 API schema，先在团队中说明影响面。

### 实现过程中

- 优先复用 `src/` 或已有服务，不在页面中复制业务规则；
- 金额、仓位、历史统计和边界判断由确定性代码完成；
- AI 只做受约束的语言理解、证据整理和白话解释；
- 每项肯定性事实都必须能回到来源、日期和覆盖范围；
- 添加功能时同步增加失败、过期、空结果和越界输入测试；
- 不因为单个用户意见立刻扩展产品范围；重复障碍才进入优先队列。

### 提交前 Definition of Done

- 用户主流程可完成，且没有新增隐藏阻断；
- 真实、缓存、演示、过期和不可用状态表达正确；
- 不出现确定性买卖建议、收益承诺或伪造证据；
- 相关自动化和评估案例通过；
- 文档、界面和代码使用同一术语；
- 没有提交秘密、个人数据、构建产物或无关文件；
- PR 说明包含：问题、实现、边界、测试结果、截图/样例和剩余限制。

## 12. 当前适合拆分的工作包

- Decision Validation：说法拆解、证据分级、原文核实、决策影响说明和红队评估；
- Deterministic Risk：持仓、行业集中度、损失情景和个人规则；
- Research Experience：研究快照、事件雷达、证据阅读和研究到决策；
- Quant Verification：候选规则确认、历史检验、统计审计和决策联动；
- User Validation：无引导任务、行为漏斗、访谈记录和课程证据；
- Reliability & Privacy：数据源状态、缓存、失败控制、密钥和匿名数据。

每个工作包应有一名主要负责人。跨包改动通过接口或 schema 协调，不通过复制代码解决。

## 13. 新组员/Codex 建议起始提示

```text
请先阅读 PROJECT_HANDOFF.md 和与你任务相关的专项交接文件。
先检查 git status 和现有实现，复述产品边界、你的负责范围及验收标准。
在得到范围确认前不要重写其他模块，不要输出买卖建议，也不要把演示或未核实资料写成事实。
实现后运行相关测试并报告剩余限制。
```

## 14. 权威资料导航

- 产品定位：`README.md`、`web/README.md`
- 冻结范围：`MVP_SCOPE.md`
- 用户流程：`CORE_USER_FLOW.md`
- Decision Layer：`DECISION_LAYER_ARCHITECTURE.md`
- Web 架构：`web/TECHNICAL_ARCHITECTURE.md`
- 产品指标：`EVALUATION.md`
- 用户测试：`USER_TEST_PLAN.md`
- 当前 README：`README.md`

当文档之间出现冲突时，优先遵循更具体、日期更新且与当前交付平台一致的文档，并在 PR 中记录采用了哪一项口径。
