# 安心看股 90 分 MVP：产品、UI/UX 与开发总提示词

> 本文件是后续设计与开发的唯一主提示词。目标不是堆出一个功能很多的股票网站，而是完成一款能交给真实用户使用、能支撑创业课演示、能证明差异化价值的桌面网页版 MVP。

## 0. 执行角色

你是一名兼具以下能力的产品负责人：

- A 股个人投资者研究流程设计；
- 金融数据产品经理；
- 信息架构与桌面工作台 UI/UX 设计；
- 数据可视化设计；
- AI/RAG/规则引擎产品设计；
- React、Next.js、TypeScript、FastAPI 工程实现；
- 金融产品合规与 AI 可靠性检查。

你的任务不是继续加入孤立功能，而是将“安心看股”完成为一条连贯的用户路径：

```text
今天发生了什么
→ 哪些变化与我的持仓和判断有关
→ 查看股票、ETF、财务和来源证据
→ 输入一笔准备执行的交易计划
→ 检查仓位、金额、证据和个人规则
→ 用户选择维持、修改或稍后再看
→ 到期后重新核实原判断
```

每一项功能都必须服务这条路径。不能直接帮助这条路径的能力不进入本轮 MVP。

---

## 1. 产品定义

### 1.1 用户

主要用户是使用电脑研究 A 股，但缺乏系统化交易纪律的普通散户和中小投资者。他们通常已经从同花顺、东方财富、雪球、微信群、小红书或新闻中获得大量信息，真正困难的是：

- 不知道哪些变化真正与自己的持仓有关；
- 容易把新闻、传闻和主观推断混在一起；
- 下单前很少计算计划后的仓位和可能损失；
- 很少写下“什么情况说明自己可能判断错了”；
- 事后难以回看当时依据和最终为什么改变计划。

### 1.2 核心价值

安心看股不是另一个荐股平台，而是连接“投资信息”和“个人决定”的工作台。

用户每天回来不是为了再读一篇 AI 总结，而是为了得到三个答案：

1. 自上次查看后，我的持仓和原判断发生了什么变化？
2. 我看到的这条消息，哪些能核实，哪些仍然只是说法？
3. 如果执行这笔计划，我的仓位、金额和个人规则会发生什么变化？

### 1.3 与竞品的区别

- 东方财富、同花顺：提供行情、资讯和交易入口；安心看股把信息与用户已记录的判断、规则和持仓影响连接起来。
- ChatGPT、Claude、Gemini：提供自然语言分析；安心看股使用真实数据、来源引用、确定性计算和连续决策记录，不让大模型凭记忆计算金融数字。
- 普通风控工具：检查账户或订单限制；安心看股在用户下单之前检查理由、证据、仓位和失效条件，但不连接券商、不执行交易。

### 1.4 使用边界

产品必须始终满足：

- 不推荐具体买卖；
- 不预测涨跌；
- 不承诺收益或胜率；
- 不连接证券账户；
- 不执行自动交易；
- 不保存券商密码、身份证、银行卡、短信验证码；
- AI 只负责理解、归纳和解释，金额、比例、FIFO、阈值冲突由确定性代码计算；
- 没有 AI Key 时，行情、持仓、ETF、财报规则、交易复盘和决策计算仍可运行。

---

## 2. 90 分产品评分标准

不得用“页面已经存在”判断完成。按下表验收，总分达到 90 才进入用户测试：

| 维度 | 分值 | 90 分要求 |
|---|---:|---|
| 核心价值清晰度 | 12 | 5 秒知道今天最值得处理的变化，30 秒理解产品与行情软件的区别 |
| 真实可用性 | 18 | 任意 A 股、真实持仓或测试 CSV 能稳定完成核心流程；无伪实时和隐藏演示数据 |
| 决策闭环 | 16 | 研究证据可进入审查；修改结果可保存；历史记录可重新核实 |
| 数据可信度 | 14 | 行情、财报、公告、ETF 披露全部显示来源、口径、更新时间和失败状态 |
| UI/UX | 14 | 首屏重点明确，中文可读，图表有效，信息密度高但不拥挤，无模板卡片墙 |
| AI 技术体现 | 10 | 结构化解析、有限 RAG、引用、规则/LLM 降级、注入防护和输出边界可演示 |
| 创新与差异化 | 8 | 展示“信息变化如何影响个人判断和计划”，不是另一份股票报告 |
| 商业验证准备 | 5 | 可记录任务完成、计划变化和付费意愿，可匿名导出 |
| 稳定性与合规 | 3 | 错误可恢复、无密钥泄漏、无荐股语言、测试通过 |

任何一项出现以下问题，整项最多得一半分：

- 用硬编码价格、公告、时间冒充实时数据；
- 页面打不开、路由割裂或嵌套 iframe；
- 核心结果只由大模型生成且无法追溯；
- 主要内容仍是长段 AI 报告；
- 用户不能完成维持、修改或延迟计划的选择。

---

## 3. 产品主路径与页面地图

### 3.1 一级导航

每日使用：

1. 工作台
2. 股票研究
3. 决策审查
4. 我的持仓

分析工具：

5. ETF 穿透
6. 交易复盘

个人记录：

7. 历史与复核
8. 我的规则

辅助页面放入账户菜单或抽屉：

- 数据与来源；
- 数据与隐私；
- 导入导出；
- 语言切换；
- 家庭访问密码。

财报体检不是孤立入口，而是“股票研究”的稳定 Tab；研究 Agent 不是孤立聊天页，而是贯穿股票研究、消息核实和决策理由的任务输入。

### 3.2 全局 Shell

所有页面共享：

- 72–80px 左侧导航栏；
- 顶部 64–72px 搜索与数据状态栏；
- 当前页名称；
- 股票/ETF 全局搜索；
- 市场开闭状态；
- 最近更新时间；
- 刷新；
- 唯一全局主操作“新建审查”。

顶部数据状态不能只是一个模糊按钮。必须显示：

`已收盘 · 行情更新于 15:02:18 · 公告更新于 14:47`

点击后打开来源抽屉：

| 数据类别 | 来源 | 状态 | 更新时间 | 口径/延迟 | 操作 |
|---|---|---|---|---|---|

状态只有：实时、延迟、部分可用、缓存、不可用。不能显示“AI 正在思考”“智能分析中”等无信息状态。

---

## 4. 全局视觉与内容规范

### 4.1 视觉场景

用户在白天或晚间坐在桌前，用 10–30 分钟检查持仓、研究一条消息或审查计划。界面应像成熟、安静的研究工作台，而不是营销网站、聊天机器人或霓虹交易终端。

### 4.2 视觉语言

- 页面背景为真正的中性浅灰或浅蓝灰，主内容白色；
- 深墨色中文正文；
- 钴蓝/靛蓝用于主操作和选择；
- 青色表示已核实资料；
- 紫色表示用户说法或个人判断；
- 琥珀表示待处理、证据不足或规则冲突；
- 红绿仅表示行情涨跌，且必须同时显示 `+/-`；
- 不限制为蓝金，不使用廉价金色描边；
- 不使用大面积渐变、玻璃拟态、霓虹发光、巨大圆角和装饰网格；
- 卡片只在信息确实需要分组时使用，禁止十几个同尺寸卡片铺满页面；
- 主要内容依靠排版、留白、分隔线和表格形成层级。

### 4.3 字体与密度

- 中文：Noto Sans SC / PingFang SC / Microsoft YaHei / system-ui；
- 页面标题 20–22px；
- 模块标题 15–17px；
- 正文 14–15px，行高 1.55；
- 辅助文字 12–13px；
- 核心数字 22–28px；
- 行情、金额、代码使用 tabular numerals；
- 正文不得低于 12px；
- 单段正文最多三行，之后转成要点、表格或折叠。

### 4.4 去 AI 味

禁止：

- “AI 一分钟总结”；
- “让 AI 帮你理性决策”；
- “开启智能投资之旅”；
- “我们不是荐股软件”；
- “系统不会替你决定”；
- 页面大标题讲产品理念；
- 长段机械的“核心洞察、综合分析、操作建议”；
- 无依据的综合评分、情绪分和“买入/持有/卖出”标签。

改写为真实任务：

- 今天发生了什么；
- 与你的持仓有关；
- 公告已核实；
- 仍未找到直接证据；
- 与上次判断相比；
- 计划后占比 31.9%，超过你的 25% 上限；
- 重新核实这条说法；
- 查看原始来源。

---

## 5. 页面一：工作台

### 5.1 页面使命

回答：

> 自从上次查看后，我的持仓、关注股票和原判断发生了什么变化？现在最值得处理什么？

这是用户形成每日使用习惯的页面，不能是功能导航页、欢迎页或数据总览模板。

### 5.2 首屏内容顺序

1. 48px 市场条：上证、沪深 300、创业板、市场状态和时间；
2. `今日变化` 主列表，占主内容约 65%；
3. `与你的持仓有关` 暴露视图，占右侧约 35%；
4. `待处理决策`；
5. `查证一条消息` 快捷输入；
6. 最近审查和完整持仓下移，不与今日变化抢第一屏。

### 5.3 今日变化列表

默认展示最多 6 条，按与用户的关联度和时间排序，不做新闻瀑布流。每条必须含：

- 股票/ETF 名称与代码；
- 时间；
- 类型：公告、财报、价格异动、成交异动、媒体、用户说法、组合重合；
- 一句话变化；
- 来源和原文；
- 影响对象：哪只持仓、哪条原判断或哪笔待审查计划；
- 关系：支持、削弱、暂不能判断；
- 动作：查看证据、重新核实、进入审查。

合格文案示例：

`生益科技回复交易所问询；披露订单需求增长，但未披露社交平台所称的具体海外客户和金额。`

不合格文案：

`AI 发现生益科技存在重大投资机会，请谨慎关注。`

### 5.4 右侧组合信息

必须显示：

- 已记录总资产；
- 当前最大单股及比例；
- 最大行业/主题暴露；
- 今日估算变化；
- 超过个人规则的持仓数；
- 最新行情覆盖率；
- 最多 3 条待处理事项。

使用水平暴露条、阈值线和数字，不使用无目标的环形仪表盘。

### 5.5 查证输入

用户可粘贴小红书、微信群、新闻或自己听到的一句话。输入后系统要求选择相关股票；随后进入股票研究的“消息核实”状态，而不是在首页直接生成长报告。

### 5.6 数据

真实数据：指数行情、用户持仓行情、持仓相关公告、用户判断复核日期、未完成审查。

禁止：静态 `7月21日`、固定股票案例、伪造“实时更新”、与用户无关的泛新闻。

### 5.7 状态

- 无持仓：解释添加持仓后能得到什么，按钮“添加持仓”；
- 无变化：显示最近成功检查时间和下一次刷新，不编造事件；
- 部分可用：明确行情成功、公告失败；
- 缓存：显示缓存时间；
- 加载：列表骨架与右侧暴露骨架分开。

### 5.8 验收

- 5 秒内找到最新变化、数据时间和关联持仓；
- 10 秒内进入任意股票研究或消息核实；
- 同一事件只出现一次；
- 没有大 Hero、欢迎口号、产品说明和同质 KPI 卡片。

---

## 6. 页面二：股票研究

### 6.1 页面使命

回答：

> 这只股票最近发生了什么？价格和成交量如何变化？哪些是正式资料，哪些仍是说法？什么会改变我的原判断？

股票研究必须让用户有探索欲望，但不能靠堆满指标或生成 40 页报告实现。

### 6.2 桌面布局

- 左侧 220–240px：持仓、关注、最近查看；
- 中间：研究主区，不低于 720px；
- 右侧 300–320px：事件详情或个人判断；
- Tab：价格与事件、财报体检、证据账本、同行比较；
- 用户切换股票或 Tab 时保持页面结构稳定。

### 6.3 股票头部

一行显示：名称、代码、市场、行业、价格、涨跌额、涨跌幅、成交额、行情时间、来源状态、关注状态。

最近交易日数据必须写 `最近交易日收盘`，不能写实时。

### 6.4 价格与事件主图

这是页面首要可视化，而不是一组装饰指标。

- 1月/3月/6月/1年；
- 有 OHLC 用 K 线，无 OHLC 用折线；
- 下方共享时间轴成交量；
- 可切换沪深300或行业指数基准；
- 公告、财报、监管、媒体和用户说法用不同形状标记；
- 标记不能只靠颜色；
- 点击事件聚焦 T-5 至 T+10 个交易日；
- 右侧同步显示事件原文、来源、发布时间和核实状态；
- 显示当日、后1日、后5日涨跌，成交量/20日均值和相对基准变化；
- 明确写“同期价格反应不代表因果关系”；
- 提供表格替代。

用户必须能回答：

1. 价格或成交量变化发生在事件前还是后？
2. 同期指数是否也发生类似变化？
3. 变化持续了多久？
4. 事件是否真正涉及用户正在核实的问题？

### 6.5 事件详情

结构固定：

- 标题；
- 来源类型；
- 发布时间；
- 原文链接；
- 原文能够确认什么；
- 原文不能确认什么；
- 与用户原判断的关系；
- 事件窗口价格数据。

不能从标题直接推导未披露事实。

### 6.6 证据账本

以表格呈现：

| 时间 | 类型 | 内容 | 来源 | 与问题关系 | 状态 |
|---|---|---|---|---|---|

类型：行情、公告、财报、监管、媒体、用户说法、个人判断。

状态：已核实、部分支持、未找到证据、相互矛盾、数据缺失。

### 6.7 个人判断侧栏

显示：

- 当前持仓金额和占比；
- 用户记录的判断；
- 判断失效条件；
- 下一次复核日期；
- 自上次记录后的新增证据；
- 买入、补仓、卖出、继续观察；
- 唯一主按钮“带着当前资料进入审查”。

没有持仓时禁用补仓和卖出，并说明原因。

### 6.8 数据与验收

- 任意 6 位 A 股代码能搜索；
- 股票名称输入有容错和候选；
- 真实行情、历史价格、公告、财务数据各自显示状态；
- 内置股票不得自动带入立场、理由和结论；
- 首屏能看到价格、时间、图表和一个真实事件；
- 任何结论能回到来源；
- 无依据的情绪分和综合分必须删除。

---

## 7. 股票研究 Tab：财报体检

### 7.1 页面使命

回答：

> 最新财报里哪些变化值得检查？利润、现金流、应收、存货和负债是否相互一致？

### 7.2 首屏

- 最新报告期；
- 数据来源和更新时间；
- 营收、归母净利润、扣非净利润、经营现金流；
- 每项显示本期值、同比和连续 6–8 季度趋势；
- 先显示异常勾稽关系，再显示全部指标。

### 7.3 四项核心检查

1. 利润增长 vs 经营现金流；
2. 营收增长 vs 应收账款；
3. 收入/成本变化 vs 存货；
4. 资产负债率 vs 经营现金流覆盖。

每项包含：公式、原始字段、本期数、对比数、触发阈值、结果、来源。

文案必须是：

`归母净利润同比增长 18.2%，经营现金流同比下降 9.4%，两者方向不一致。`

不能是：

`盈利质量较差，建议卖出。`

### 7.4 同行比较

只在行业分类、报告期和字段口径一致时显示。比较 ROE、毛利率、经营现金流/净利润、资产负债率。覆盖不足时显示原因，不能生成假排名。

### 7.5 验收

- 至少支持真实股票最近 4 个报告期；
- 显示字段口径；
- 所有异常可展开追溯；
- 不预测下一期利润；
- 不配置 AI Key 仍能生成规则体检结果。

---

## 8. 页面三：新建决策审查

### 8.1 页面使命

回答：

> 我准备做什么？为什么现在做？这笔计划会如何改变我的仓位？什么情况说明判断可能错了？

### 8.2 页面结构

- 左侧 60–64%：渐进式输入；
- 右侧 36–40%：实时数字影响；
- 保持一个页面，不做五屏向导；
- 不向普通用户展示 JSON、RAG、Prompt 等技术词。

### 8.3 计划输入

- 股票；
- 买入/补仓/卖出/继续观察；
- 计划金额；
- 当前持仓自动读取；
- 从研究页进入时带入股票、用户正在核实的问题和已选证据。

输入金额后 300ms 内更新：

- 当前金额 → 计划后金额；
- 当前比例 → 计划后比例；
- 个人上限；
- 超出差值；
- 下跌 10%/20%/30% 的金额情景。

### 8.4 理由输入与结构化确认

用户用自然语言输入：

`已经跌了很多，朋友说公司有海外大订单，我想补仓五万元。`

解析为可编辑结构：

- 可核实事实；
- 未核实外部说法；
- 主观推断；
- 紧迫性表达；
- 信息来源；
- 预计期限；
- 判断失效条件。

每项都能修改、删除或重新分类。用户确认后才进入证据检索和最终审查。

### 8.5 失效条件

必须让用户填写：

- 哪个经营、财务、价格或事件条件会使原判断需要重查；
- 预计观察期限；
- 下一次复核日期。

提供句式帮助，不代替用户决定：

`如果下一期经营现金流没有改善，或正式披露仍未出现相关订单，我会重新检查。`

### 8.6 右侧实时反馈

固定三个模块：数字影响、规则差值、资料完成度。按钮文案随状态改变：

- 还需填写失效条件；
- 先确认理由拆解；
- 正在核实 1 条外部说法；
- 生成审查结果；
- 保存并稍后继续。

### 8.7 验收

- 新用户 60 秒内能完成一笔计划；
- 不能在必要字段缺失时伪装完成；
- 用户能看懂系统如何理解原话；
- 无 AI Key 时使用规则和关键词解析降级，并明确标记；
- 不允许模型决定用户适合的仓位比例。

---

## 9. 页面四：决策审查结果

### 9.1 页面使命

回答：

> 这笔计划具体触发了什么？差多少？哪些依据已经核实？我要维持、修改还是稍后再看？

### 9.2 首屏

第一句话必须是数字事实：

`本次补仓会使单股占比从 18.6% 升至 31.9%，超过你的 25% 上限；1 条外部说法仍未找到正式披露支持。`

然后展示：

1. 仓位前后 bullet chart；
2. 下跌 10%/20%/30% 金额条；
3. 理由证据构成；
4. 规则问题表；
5. 修改前后比较。

### 9.3 规则问题表

| 检查项 | 当前计划 | 个人规则/证据要求 | 差值 | 可执行动作 |
|---|---|---|---|---|

例如：

`单股仓位 | 31.9% | ≤25% | +6.9个百分点 | 修改金额或保留并记录理由`

### 9.4 证据对应

左侧保留用户原话；右侧逐句对应：事实、推断、未核实信息、来源。没有证据时写清检索范围和时间：

`截至 14:32，在 2 条正式披露和 3 条公开媒体资料中未找到“海外大订单”的直接证据。`

### 9.5 用户操作

底部固定：

- 维持原计划并记录理由；
- 修改计划；
- 稍后再看。

修改金额后，所有数字与冲突即时更新，并显示原计划/修改后。任何选择都由用户点击，不能自动“放行”或“禁止”。

### 9.6 验收

- 30 秒内说出触发规则、差值、情景损失和未核实信息；
- 不显示综合风险分、买卖建议和收益预测；
- 选择后保存规则快照、证据快照、原金额、最终金额、耗时和复核日期；
- 刷新后记录仍存在。

---

## 10. 页面五：我的持仓

### 10.1 页面使命

回答：

> 我的资金主要暴露在哪里？哪些持仓超过个人边界？今天哪些持仓出现了需要查看的变化？

### 10.2 首屏

- 记录资产；
- 已投资金额；
- 行情覆盖率；
- 最大单股及比例；
- 最大行业/主题暴露；
- 今日估算变化及时间；
- 水平持仓分布和个人上限线。

### 10.3 持仓表

| 股票 | 最新价/时间 | 持仓金额 | 资产占比 | 今日变化 | 个人边界 | 新事件 | 操作 |
|---|---|---:|---:|---:|---|---|---|

操作：研究、审查、编辑。超限显示具体差值。

### 10.4 行业与主题暴露

使用排序水平条，显示金额、比例和数据覆盖。未知行业保持“未知”，不强行归类。行业映射来源必须可查看。

### 10.5 导入导出

- CSV 上传；
- CSV 粘贴；
- 手工输入；
- 导入预览；
- 字段错误逐行提示；
- CSV 备份与恢复。

默认只需要股票代码、名称和持仓金额；允许使用模拟金额。

### 10.6 验收

- 上传后所有页面共用同一持仓数据；
- 工作台、研究、审查中的金额一致；
- 用户一眼找到最大暴露和超限持仓；
- 无持仓时不显示样例投资组合。

---

## 11. 页面六：ETF 穿透

### 11.1 页面使命

回答：

> 我持有的 ETF 是否买重复了？底层真实暴露集中在哪些股票、行业和主题？披露信息有多旧？

### 11.2 输入与结果

- 搜索或从持仓加入 ETF；
- 最多 10 只；
- 每只输入金额或组合权重；
- 左侧组合输入，右侧诊断结果；
- 不使用 iframe，必须成为统一 React 页面。

### 11.3 结果首屏

- ETF 数量；
- 已取得底层成分数量；
- 披露覆盖率；
- 最近/最旧披露日期；
- 重复股票数量；
- 一句话具体结论。

### 11.4 可视化

1. 行业/主题暴露水平条；
2. ETF × 重复股票矩阵；
3. 重复股票排序表；
4. 每只 ETF 披露日期和覆盖率表。

必须标记：

`持仓来自截至 YYYY-MM-DD 的定期披露，不代表当前实时持仓。`

### 11.5 连接决策

用户可从重复暴露结果进入持仓或审查，例如：

`这笔新增 ETF 计划会使半导体主题暴露从 24% 升至 37%。`

系统不输出“应卖掉哪只 ETF”。

### 11.6 验收

- 20 秒找到重合最高的股票；
- 所有权重有披露日期；
- 无披露时不使用演示成分；
- ETF 数据与个人持仓金额连接。

---

## 12. 页面七：交易复盘

### 12.1 页面使命

回答：

> 已实现盈亏来自哪些完成交易？当前还有哪些未平仓数量？交易记录触发了哪些确定的个人规则？

### 12.2 输入流程

1. 上传/粘贴 CSV；
2. 显示前 10 行预览；
3. 字段映射；
4. 显示接受、忽略、错误行数；
5. 用户确认；
6. 执行 FIFO 与规则检查。

### 12.3 结果首屏

- 数据区间；
- 总买入；
- 总卖出；
- 已实现盈亏；
- 手续费；
- 未平仓标的；
- FIFO 口径；
- 数据状态。

### 12.4 结果内容

- 按股票已实现盈亏排序条形图；
- 每笔 FIFO 匹配表；
- 未平仓数量表；
- 交易时间线；
- 规则触发：单股集中、频繁交易、亏损后短时补仓、金额超限、字段缺失。

合格文案：

`同一股票在亏损卖出后 4 小时内重新买入，触发你设置的 24 小时冷静期。`

禁止：

`你存在报复性交易心理。`

### 12.5 AI 报告

作为次级 Tab，只能归纳已计算结果；显示模型、时间和引用的数据字段；不能增加未计算事实。Mock 或无 Key 时显示规则版报告。

### 12.6 连接规则

用户可以把复盘发现转成个人提醒规则，但必须确认后保存。例如：

`将“亏损卖出后 24 小时内重新买入”加入提醒。`

### 12.7 验收

- 测试 CSV 可完整跑通；
- 不配置 AI Key 仍能复盘；
- 已实现和未实现不混淆；
- 原生页面，不使用 iframe；
- 可导出报告和标准化交易 CSV。

---

## 13. 页面八：历史与复核

### 13.1 页面使命

回答：

> 我过去审查过什么？哪些计划被修改或延迟？哪些原判断现在到了重新核实的时间？

### 13.2 页面主体

以表格为主：

| 时间 | 股票 | 操作 | 原金额 | 最终金额 | 选择 | 触发规则 | 未核实信息 | 下次复核 |
|---|---|---|---:|---:|---|---|---|---|

筛选：股票、日期、操作、选择、是否修改、是否到期。

点击一行打开侧边详情，包含：

- 原始理由；
- 当时结构化理解；
- 当时证据与来源；
- 原计划/最终计划；
- 当时规则快照；
- 后续新增资料；
- 用户复核记录。

### 13.3 顶部概览

只显示：

- 完成审查次数；
- 修改或延迟比例；
- 最常触发规则。

禁止理性指数、冲动分、投资人格等心理标签。

### 13.4 重新核实

到期记录必须有“重新核实”动作，自动载入当时问题和最新公开资料，显示：

`新证据是否改变原判断？`

### 13.5 验收

- 历史不是只读墓地；
- 原计划与最终计划差异突出；
- 旧证据快照不被最新数据覆盖；
- 可匿名导出用户测试 CSV。

---

## 14. 页面九：我的规则

### 14.1 页面使命

回答：

> 我希望系统在什么具体情况下提醒我重新检查？这些规则会如何影响当前持仓和下一笔计划？

### 14.2 首次设置

入口：强提醒模板、标准提醒模板、用一句话自定义。模板不是风险测评或投资建议。

自然语言示例：

`我大约用 20 万元投资，单只股票不超过 5 万元；亏损后想补仓时，希望隔一天再看。`

解析后逐项确认：

| 用户原话 | 系统理解 | 当前值 | 是否明确 | 修改 |
|---|---|---:|---|---|

用户确认前不能保存。

### 14.3 已设置规则

| 规则 | 当前值 | 最近触发 | 对当前持仓影响 | 操作 |
|---|---:|---:|---|---|

规则至少包括记录资金、单股金额上限、单股比例上限、单笔提醒金额、冷静期、失效条件要求。

### 14.4 影响预览

修改规则时即时显示：

- 当前哪些持仓超过新边界；
- 最近哪些计划会被触发；
- 保存前后差异。

### 14.5 验收

- 2 分钟完成首次设置；
- 不要求用户理解专业风险术语；
- 模型不能替用户决定合适比例；
- 保存后下一笔审查立即使用新规则。

---

## 15. 辅助页面：数据、隐私与语言

### 15.1 数据与来源

列出行情、公告、新闻、财报、ETF 持仓、AI 模型的提供方、更新时间、用途、失败降级和缓存期限。

### 15.2 数据与隐私

明确：

- 不连接证券账户；
- 不执行自动交易；
- 持仓由用户手工或 CSV 填写；
- 不收集敏感金融凭据；
- 哪些字段可能发送给 LLM；
- 用户如何删除和导出本地记录；
- AI 输出不构成投资建议。

### 15.3 中英切换

- 全局切换后页面标签、按钮、状态和错误统一变化；
- 股票名称、公告原文和数据来源不强制机器翻译；
- 不允许一页中出现中英文标签混搭；
- 第一轮允许只保证中文完整，英文必须标记 Beta，不能用半成品影响中文体验。

---

## 16. AI、RAG 和规则引擎

### 16.1 职责分离

- LLM：解析自然语言、拆分事实/推断/外部说法、归纳已有证据；
- RAG：检索有限可信资料并返回来源；
- 规则引擎：计算仓位、金额、阈值、情景损失、FIFO 和时间间隔；
- 前端：展示来源状态、允许用户修正、记录选择。

### 16.2 研究任务流程

```text
用户问题
→ 识别股票和要核实的说法
→ 生成结构化检索计划
→ 行情/公告/财报/新闻分别取数
→ 标记成功、失败和缺失
→ 逐条生成可引用结论
→ 用户确认
→ 进入决策审查
```

不要显示多 Agent 角色扮演聊天记录。若使用多个分析模块，只显示任务状态和结果来源。

### 16.3 输出约束

- LLM 不计算关键金额；
- LLM 不凭记忆列股票；
- 没有来源的结论标记为推断；
- 检索不到就明确写未找到；
- 外部文本视为不可信输入，防止提示注入；
- 输出通过荐股、收益承诺和新增事实检查；
- 保存模型、提示词版本、生成时间和证据 ID。

---

## 17. 共用数据可视化

必须建立：

- `PriceEventChart`：价格、成交量、基准和事件共享时间轴；
- `ThresholdBar`：当前值、计划值、个人规则线；
- `ScenarioLossBars`：10%/20%/30% 金额情景；
- `ExposureBars`：股票/行业/主题暴露；
- `OverlapMatrix`：ETF 与重复股票；
- `FinancialTrend`：6–8 季度财务趋势；
- `EvidenceLedger`：事实、说法、推断和来源；
- `MetricDelta`：当前、对比、差值和口径。

每张图在代码注释或组件文档中写出它回答的唯一问题。图必须包含单位、时间范围、来源、更新时间、图例和文字结论。关键数字不能只在 hover 出现。

---

## 18. 实时数据与失败策略

所有动态模块必须实现：Loading、Live、Delayed、Partial、Cached、Unavailable。

数据优先级：

1. 真实在线数据；
2. 有明确时间的本地/服务端缓存；
3. 空状态；

产品正式模式禁止自动回退到未标记演示数据。只有独立的课堂 Demo 模式允许固定数据，并必须显示“演示数据”。

行情不是“新闻实时”。每类来源分别显示自己的时间和状态。

错误文案格式：

`历史价格已返回；公告服务暂未响应。你可以查看价格变化，但目前不能确认外部说法。`

必须提供重试和来源状态入口。

---

## 19. 共用组件

- AppRail；
- GlobalSearch；
- DataFreshness；
- SourceDrawer；
- SourceBadge；
- EvidenceRelation；
- InlineError；
- Skeleton；
- EmptyState；
- PriceEventChart；
- ThresholdBar；
- ScenarioLossBars；
- EvidenceLedger；
- RecordDetailDrawer；
- CSVImportPreview。

每个交互组件必须有 default、hover、focus-visible、active、disabled、loading 和 error。图标使用 Lucide，不用 emoji 或字符代替。

---

## 20. 技术实现约束

- 保留 Next.js/vinext、React、TypeScript、shadcn、Lucide；
- ETF 和交易复盘改成原生 React 页面，禁止 iframe；
- 初始关键数据优先服务端读取；
- 慢数据使用 Suspense 或局部骨架；
- 定时刷新不导致整页重绘；
- 动态区域预留尺寸，CLS < 0.1；
- 真实数据、缓存和演示数据使用不同类型，不允许在 UI 层混淆；
- 业务计算从页面组件分离；
- 密钥只在服务器端环境变量或 secrets；
- 不为了未来移动端牺牲当前桌面信息架构。

---

## 21. 实施顺序

### P0：可交给用户前必须完成

1. 全局 Shell、来源状态和搜索；
2. 工作台真实变化；
3. 股票研究价格—事件联动；
4. 财报体检基础版；
5. 新建决策和结果；
6. 持仓 CSV/手工录入并全局联动；
7. 历史保存和重新核实；
8. 无 AI Key 降级；
9. 真实状态、空状态和失败状态；
10. 桌面浏览器验收。

### P1：提高完整度和留存

1. ETF 原生整合；
2. 交易复盘原生整合；
3. 规则自然语言设置；
4. 同行比较；
5. 研究任务与逐结论引用；
6. 匿名用户测试数据导出；
7. 中英文完整切换。

### P2：本轮不做

- 自动交易；
- 券商账户连接；
- 自然语言选股；
- 完整量化回测；
- 全网社交平台爬取；
- 原生 App、小程序、PWA；
- 支付系统；
- 大规模多用户架构；
- 心理健康诊断；
- AI 涨跌预测。

---

## 22. 逐页执行方法

严格一次完成一页：

1. 写出该页唯一问题；
2. 列出用户进入页面时已有的数据；
3. 生成 2–3 张风格和信息拓扑明显不同的高保真效果图；
4. 由用户选择方向；
5. 在 Figma 中建立可编辑布局和组件；
6. 明确 API 与字段；
7. 实现真实默认状态；
8. 实现加载、空、部分失败、缓存和不可用；
9. 浏览器以 1440×900、1280×800、1100×800 检查；
10. 模拟真实用户完成任务；
11. 运行自动化测试；
12. 对照本文件验收后才进入下一页。

视觉效果图不能直接当开发稿；Figma 不能只画没有真实数据状态的静态画面；代码不能在视觉方向未确认时大规模重写。

---

## 23. 每页交付物

每页必须同时交付：

1. 页面目标；
2. 高保真效果图；
3. Figma 可编辑稿；
4. 数据字段与来源；
5. 默认/加载/空/部分/缓存/失败状态；
6. 交互说明；
7. 真实代码；
8. 浏览器截图；
9. 自动化测试；
10. 用户任务模拟记录；
11. 本页验收清单。

---

## 24. 90 秒课堂 Demo

演示必须使用稳定固定场景，但醒目标记演示数据：

1. 工作台看到“朋友称公司有海外大订单”的待核实消息；
2. 进入生益科技研究；
3. 价格—事件图显示公告、传闻和成交量位置；
4. 证据账本显示公告未披露具体客户和金额；
5. 进入补仓 ¥50,000 的审查；
6. 仓位从 18.6% 升至 31.9%，超过 25% 上限；
7. 显示下跌 20% 的金额影响；
8. 用户把金额改为 ¥10,000 或稍后再看；
9. 冲突消失并保存记录；
10. 历史页显示修改前后差异和下次复核点。

演示不能依赖在线行情或 AI Key，但正式模式不能把这套数据冒充实时数据。

---

## 25. 用户测试数据

匿名记录：

- 测试者编号；
- 是否找到最新变化；
- 是否理解来源状态；
- 是否完成股票研究；
- 是否识别事实和传闻；
- 原计划金额；
- 触发规则；
- 最终选择；
- 最终金额；
- 完成时间；
- 风险认可度；
- 再次使用意愿；
- 付费测试意愿；
- 最困惑步骤。

不得记录姓名、身份证、银行卡、券商账户和密码。

---

## 26. 最终拒绝清单

出现任一项必须退回：

- 首屏是大标题、口号或功能介绍；
- 页面靠十几个同质卡片显得丰富；
- 主要内容是长段 AI 总结；
- 真实数据和样例数据混排；
- 股票、ETF、复盘页面使用 iframe；
- 没有来源、时间和失败状态；
- 9–11px 大量正文；
- 无意义仪表盘、情绪分、风险分、买卖信号；
- 事件和股价只靠无法点击的小圆点关联；
- 把同期变化描述为因果；
- 把朋友说、新闻很多或社交传闻当成事实；
- 用红绿表示普通按钮和风险等级；
- 重复显示产品边界和免责声明；
- 页面中出现 JSON、RAG、Agent、Prompt 等面向开发者的语言；
- 数据失败时自动显示未标记演示结论；
- 用户不能修改 AI 的结构化理解；
- 模型直接计算仓位、FIFO 或金额；
- 用户不能完成维持、修改或延迟；
- 功能看似很多，但不能从研究进入决策并保存历史。

---

## 27. 当前第一步

不要立即重写所有代码。

先完成“工作台”的 3 张高保真效果图，三张必须在信息拓扑上真正不同：

1. 时间线主导：以与用户相关的变化流为主；
2. 持仓影响主导：以组合暴露和变化影响矩阵为主；
3. 任务主导：以待核实消息、到期判断和待审查计划为主。

三张图都必须使用真实产品内容，展示数据时间、来源状态、持仓影响和可执行动作；禁止只更换颜色。用户确认后，才进入 Figma 和工作台实现。
