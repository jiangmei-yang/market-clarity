import {NextResponse} from "next/server";import {applyQuantWorkspace} from "@/app/lib/quant-research-server";
export async function POST(request:Request){try{const body=await request.json() as {task_id?:string;confirmed?:boolean};if(body.confirmed!==true||!body.task_id)return NextResponse.json({message:"请明确确认工作台修改"},{status:422});return NextResponse.json({status:"applied",...(await applyQuantWorkspace(body.task_id,true))});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法应用工作台"},{status:409});}}
