import {NextResponse} from "next/server";import {previewDashboard} from "@/app/lib/dashboard-server";export async function POST(request:Request){try{const body=await request.json() as {workspace_id?:string;instruction?:string};if(!body.workspace_id||!body.instruction)return NextResponse.json({message:"缺少 workspace_id 或 instruction"},{status:422});return NextResponse.json({patch:await previewDashboard(body.workspace_id,body.instruction),requires_confirmation:true});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法生成预览"},{status:422});}}
