# 安心看股桌面 UI 开发规范

## 已确认的产品界面方向

Figma 方向稿：https://www.figma.com/design/gmPwsqOhhiLxEJUIG7Bnh6

- 工作台采用“变化优先”：第一屏先回答今天发生了什么、影响哪些持仓、有哪些待处理决定。
- 股票研究采用“事件驱动”：价格、成交量、基准走势和公告/传闻事件必须出现在同一时间轴。
- 决策审查采用“数字与规则优先”：先展示仓位变化、情景损失和规则冲突，再展示解释文字。
- 三种布局属于同一产品的三个任务界面，不把所有页面强行套成相同的卡片仪表盘。

## 设计令牌

- 页面背景：`#F4F7FB`；主要表面：`#FFFFFF`；深色导航：`#10192A`。
- 主文字：`#162033`；次级文字：`#667085`；边界：`#DCE3EE`。
- 主操作：`#3157D5`；已核实事实：`#078C99`；个人判断/待核实：`#6E59CF`；需要关注：`#B7791F`。
- 红绿只表达市场涨跌与数值变化；提醒、规则冲突和产品按钮不得借用红绿。
- 中文字体使用 Noto Sans SC；正文不低于 13px，标签不低于 11px，关键数字 20–26px。
- 圆角以 10/12/16px 为主；避免每一层都套卡片，优先通过留白和 1px 分隔线建立层级。

## 页面信息优先级

### 工作台

1. 与用户相关的今日变化。
2. 变化影响的持仓、关注股票或待审查计划。
3. 组合快照和待处理决策。
4. “粘贴一条消息并查证影响”的快速入口。

禁止使用大幅欢迎 Hero、产品口号、虚构 AI 旁白和无意义动态数字。

### 股票研究

1. 股票、价格、涨跌和数据时间。
2. 价格—成交量—事件共同时间轴。
3. 当前选中事件的来源、可核实内容和事件窗口表现。
4. 证据账本：已核实、待核实、个人判断。
5. 财务体检、公告和同行比较作为相邻任务标签，而非首屏信息堆叠。

事件窗口默认展示 T-5 至 T+10 个交易日；至少给出公告后 1 日/5 日变化、成交量相对 20 日均值、相对沪深 300 或行业基准的变化。页面必须明确说明同期变化不等于因果关系。

### 交易前审查

1. 一句话数字结论和当前状态。
2. 补仓前后仓位、个人阈值及下跌情景金额。
3. 理由中的事实、待核实信息和主观推断。
4. 个人规则冲突。
5. 原计划与修改方案的前后比较。
6. 修改、稍后再看、维持并记录理由三个用户动作。

不输出“应买、应卖、放行、禁止”等替用户做决定的文案。

## 结构

- `AppRail`：稳定主导航与账户入口。
- `ContextRail`：工作台任务、研究股票列表；决策页按需要隐藏。
- `Workspace`：每页一个连续工作画布，以分隔线形成章节。
- `InsightAside`：操作情境、规则与证据，不使用弹窗打断主流程。
- `DecisionBar`：决策页底部操作栏，滚动时保持可见。

## 交互验收

- 导航、股票切换、操作类型、金额输入与决策结果均为真实可点击状态。
- 金额变化即时更新仓位、超限差值和下跌情景，计算结果使用等宽数字。
- 研究页进入决策时保留股票与操作类型。
- 键盘可遍历所有主要操作；焦点轮廓清晰；图标按钮有可读标签。
- 无数据、部分来源失败与未配置 AI 时保持页面结构，并在局部说明状态。
- 所有行情、公告和事件必须显示来源状态与更新时间；演示或模拟数据必须显式标记。
- 点击图表事件后，右侧详情、事件窗口指标和证据账本同步聚焦。
- 图表不依赖鼠标悬停才能理解；关键事件标签和基准图例始终可见。

## 实现约束

- React 客户端状态仅用于原型交互；不新增后端或部署工作。
- 优先组合 shadcn/ui 的 Button、Badge、Tabs、Table、Alert、Separator、Input、Textarea 与 Tooltip。
- 自定义样式只负责产品布局、数据可视化和设计令牌；组件颜色使用语义变量。
- 统一使用 Lucide 线性图标，16/18/20px 三档，禁止字符图标和 emoji。
- 1440×900 为视觉验收尺寸，同时检查 1100px 以上无横向溢出。
- 使用 tabular numerals 展示行情、金额和比例，避免数字变化造成布局跳动。
- 单页最多一个主按钮；同层级信息不超过 3 种强调色。
