# AI 决策辅助提示词架构

## 职责边界

安心看股先由普通代码计算金额、FIFO、仓位、集中度、费用、回撤和规则触发；AI 只解释这些结构化事实、提出需要补充的问题和组织复盘语言。无 API Key 时使用相同字段的确定性 Mock。

## 四类任务

| 任务 | 输入 | 输出 | 接口 |
|---|---|---|---|
| 交易前防错 | 规则引擎已计算的计划后占比、相似资产、用户理由和期限 | 理由类型、缺失信息、风险解释、确认问题 | `POST /pre-trade/check-ai` |
| 交易后复盘 | FIFO、金额、费用、暴露、行为标签和主要驱动 | 事实、行为信号、知识解释、下次检查项、完整报告 | `POST /report/generate_ai`、`POST /attribution/run_with_ai_report` |
| 金融指标解释 | 指标、数值、参考和相关资产 | 一句话解释、重要性、当前含义、核对问题 | `POST /metrics/explain-ai` |
| 持仓风险解释 | 已计算的组合暴露、重合、波动率、回撤和风险标签 | 主要暴露、重复、仓位风险、行业风险、定期检查项 | `POST /portfolio/explain-ai` |

## 安全约束

- 系统提示词位于 `src/services/ai_report.py` 的 `SYSTEM_PROMPT`。
- 模型不能补造行情、财报、新闻或收益。
- 模型不能计算或覆盖确定性规则结果。
- 输出不包含目标价、胜率、买卖指令或收益承诺。
- 数据缺失时明确返回“暂无数据”或“暂无足够数据判断”。
- 所有输出包含与任务对应的免责声明。

## 前端使用

复盘接口同时保留旧字段 `ai_report`，并新增 `summary`、`facts`、`behavior_signals`、`knowledge_explanations` 和 `next_checklist`。旧客户端可继续显示完整字符串，新页面应优先分别展示结构化区块。
