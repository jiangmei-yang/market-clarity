# 工作台实现规范（方向 E · 2026-07-22）

## 1. 页面任务

工作台不是产品介绍页，也不是数据总览页。用户打开后必须在 5 秒内回答：

1. 现在是否有一件必须先处理的事；
2. 自上次查看后，哪些真实变化与自己的持仓有关；
3. 组合暴露是否接近或超过个人边界。

视觉基准：`design-concepts/workbench/workbench-e-editorial.png`。实现吸收其信息架构和编辑式排版，不逐像素复制图片中的虚构数据。

## 2. 首屏信息顺序

### A. 现在先处理

只显示一件事，优先级如下：

1. 最近未完成或仍有冲突的交易计划；
2. 当前最大单股持仓超过个人比例或金额上限；
3. 持仓中出现高优先级正式披露或单日显著波动；
4. 已有持仓但尚未建立判断；
5. 尚无持仓时，引导导入持仓；
6. 以上都没有时，显示“当前没有需要立即处理的事项”。

必须同时展示：对象、触发原因、一个关键数字、一个主操作。不得用长段产品说明。

### B. 与你有关的变化

主内容区使用时间线/编辑列表，不使用卡片墙。每条只显示：

- 时间；
- 股票名称；
- 来源类型与来源名；
- 一句话事实；
- 与持仓/原判断的关系；
- 来源链接或进入股票研究。

只使用本次真实取得的公告与行情。没有数据时明确显示空状态、失败状态或部分可用状态。

### C. 组合暴露

右侧固定窄栏，展示：

- 最大 6 个持仓的占记录资金比例；
- 个人单股比例上限虚线；
- 超限项使用琥珀色，不使用红色；
- 数据覆盖数与更新时间；
- 进入持仓页的入口。

本轮没有可靠行业分类时不得伪造行业暴露，明确标注“按单股持仓”。ETF 穿透和行业归类完成后再切换为行业/主题视图。

## 3. 删除或下移

- 删除工作台专属的“今日节奏”第二侧栏；全局左侧导航已经足够。
- 删除首屏“第一次使用”“约 1 分钟”“系统会……”等产品解释。
- 删除重复的三项数据指标条；覆盖信息合并到右栏标题。
- 删除“数据覆盖”独立卡片；逐只覆盖状态并入组合暴露行和页脚。
- “最近决策”下移到首屏之后，仅保留最多三条。
- 完整持仓金额、导入和边界说明留在“我的持仓”。

## 4. 组件拆分

- `WorkbenchPriority`: 根据持仓、规则、最近审查和变化生成唯一优先事项。
- `ChangeTimeline`: 真实变化列表及 loading/empty/partial/error。
- `PortfolioExposure`: 单股暴露条、个人边界、数据覆盖。
- `WorkbenchMessageCheck`: 底部“核实一条消息”输入，提交后进入对应股票研究。
- `RecentTable`: 首屏以下的最近记录。

本轮先保留在 `app/page.tsx`，待交互稳定后再拆文件，避免为架构重写拖慢验证。

## 5. 数据映射

| 界面字段 | 当前来源 | 状态规则 |
|---|---|---|
| 持仓名称/金额 | `localStorage` 持仓 | 本地数据 |
| 价格/涨跌 | `/api/information/{code}` | 实时、部分、不可用 |
| 公告 | `/api/evidence/{code}` | 显示来源、日期、链接 |
| 个人比例上限 | `UserRules.maxSingleStockRatio` | 用户确认规则 |
| 单股金额上限 | `UserRules.maxSingleStockValue` | 用户确认规则 |
| 最近审查 | 本地决策记录 | 本设备记录 |
| 行业暴露 | 本轮不展示 | 缺少可靠归类，不推断 |

## 6. 状态文案

- Loading：`正在检查 3 只持仓的行情与公告`
- Ready：`更新于 09:41 · 3/3 只持仓已取得资料`
- Partial：`部分可用 · 2/3 只持仓已取得资料`
- Error：`公开资料暂时不可用` + `重试`
- No holdings：`先导入持仓，工作台才知道哪些变化与你有关`
- No changes：`已完成检查，当前没有需要展开的变化`

禁止：`AI 正在分析`、`智能洞察`、`开启理性投资`、`核心洞察`。

## 7. 桌面布局

- 1440px：72px 全局导航 + 主内容；变化区约 68%，暴露区约 32%。
- 1280px：保持双栏，右栏不低于 300px。
- 1100px：主内容仍双栏，文字收紧，不允许横向溢出。
- 主面板不使用外围大圆角容器；依靠背景、分隔线和留白分区。
- 正文不低于 12px，主要事实 14px，页面段落最多两行。

## 8. 本页验收

- 不滚动即可看见唯一优先事项、至少两条变化的位置和组合暴露顶部。
- 用户无需阅读产品理念即可知道下一步操作。
- 页面不存在硬编码行情、公告数或“今天”日期。
- 任意持仓数量 0、1、5，以及全部数据源失败时页面结构都成立。
- 键盘可访问所有按钮和来源链接。
- 通过构建、现有渲染测试和浏览器 1440×900 人工体验。
