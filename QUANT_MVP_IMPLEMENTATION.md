# 安心看股量化 MVP 实现说明

## 产品边界

这一期把用户自己的投资想法转换为可确认、可筛选、可验证的规则。它不自动下单、不自动调仓、不预测涨跌，也不提供“最优参数”、收益承诺或买卖指令。

## 已完成闭环

1. 中文规则解析：识别基本面、估值和技术条件；模糊阈值返回澄清问题。
2. 规则确认门：保存和修改都会回到草稿；只有再次确认后才参与筛选与提醒。
3. 逐条件筛选：结果显示通过、未通过、数据缺失、数据日期、定义和来源。
4. 历史策略验证：包含样本内外、最大回撤、年化收益/波动、Sharpe、Sortino、换手、成本和参数敏感性。
5. 组合风险：计算单一持仓、行业集中、主题重复以及行业冲击的线性敏感性。
6. 规则提醒：只提示规则偏离和数据缺失，明确 `not_trade_instruction=true`。
7. 桌面工作台：`/quant` 提供“我的规则、条件筛选、策略验证、组合风险、规则提醒”五个任务视图。

## 数据与口径

- 股票筛选：固定演示股票池；财务日期为 2025-12-31，价格类日期为 2026-07-21。
- 历史验证：固定可复现演示样本，前复权，显式加入佣金、印花税和滑点。
- 组合风险：用户输入金额 + 演示行业/主题映射；缺少历史序列时最大回撤显示暂无数据。
- 缺失数据：状态为 `unknown`，不补造，也不作为通过。

所有响应都携带 `data_status`，并明确标注 `live/cache/demo`、日期、来源和限制。

## 当前未实现

- 真实全市场批量扫描；
- 完整退市股票历史池与无幸存者偏差回测；
- 实时规则推送和外部通知；
- 自动交易、自动调仓、预测模型和无限参数搜索；
- 使用完整收益序列估算组合协方差、VaR 或真实历史最大回撤。

这些限制是有意的：先验证用户是否愿意创建、确认和复核自己的规则，再扩大数据覆盖。

## 本地访问

```bash
.venv/bin/uvicorn api:app --reload --host 127.0.0.1 --port 18000
```

打开 `http://127.0.0.1:18000/quant`。桌面 Sites 前端使用 `cd sites_frontend && npm run dev`，访问其 `/quant` 路由。
