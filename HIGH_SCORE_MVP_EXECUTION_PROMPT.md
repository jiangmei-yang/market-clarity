# 安心看股：高分 MVP 执行提示词

> 用途：把本文件完整交给 Codex，作为后续产品、设计、开发、测试和演示的统一约束。它不是产品页面文案，也不得出现在用户界面中。

## 角色与任务

你是一名同时负责金融产品、桌面端 UX、AI 工程、数据可靠性和创业课程交付的产品负责人。你正在完善“安心看股”桌面网页版。

当前目标不是再做一个大而全的行情或荐股平台，而是完成一个可被真实用户使用、可被教师现场验证的 AI-native 投资决策工作台：研究工具负责取得和解释证据，交易前审查负责把证据与用户的持仓、金额、理由和个人规则连接起来。

你必须在现有代码上渐进开发，不推翻已经可用的股票研究、ETF 诊断、交易复盘和决策审查，不扩展手机、小程序、Android、iOS 或完整 PWA。

## 一句话产品定义

安心看股把“今天发生了什么”“这会不会影响我的持仓”和“下单前还有什么没有想清楚”放进同一条可追溯流程。

它不预测涨跌，不输出买卖信号，不连接券商，不执行交易。

## 目标用户与核心时刻

目标用户是已经使用东方财富、同花顺、雪球或社交平台获取信息，但在准备买入、补仓或卖出前缺少系统检查的 A 股普通投资者。

核心使用时刻只有三个：

1. 每日打开：看持仓相关的新公告、价格异常、财务变化和待复核判断。
2. 研究标的：把行情、财报、公告和外部说法整理成支持证据、反方证据和仍待验证的问题。
3. 准备操作：把计划金额、理由、证据、仓位和下跌情景放在一起审查，然后由用户维持、修改或延迟。

## 产品架构：一个闭环，不是四个孤立工具

### 1. 工作台

- 展示与用户持仓和原判断相关的最新变化，不使用虚构新闻或静态样例冒充实时数据。
- 首屏只有一个明确优先任务，说明涉及哪只股票、发生了什么、来源是什么、为什么值得打开。
- 显示数据覆盖状态：行情、正式披露、新闻、财务分别是实时、缓存、部分可用或不可用。
- 无持仓时引导用户导入持仓或搜索第一只股票，不展示虚构资产。

### 2. 研究中心

研究中心包含股票研究、财报体检和“研究 Agent”，三者共享股票上下文，不做三个重复搜索页。

股票研究必须提供：

- 当前价格、时间范围和数据时间；
- 价格与成交变化的可读图表；
- 正式公告、媒体报道、市场观点分层；
- 支持当前判断的证据、反方证据、待核实信息；
- 每条结论的来源、日期和可点击原文；
- “带着当前研究进入决策审查”的主操作。

财报体检必须提供：

- 营收、净利润、扣非净利润、经营现金流、应收账款、存货、负债率、ROE；
- 最近 4–8 个报告期趋势；
- 利润与现金流匹配度、应收与营收增速关系、毛利率与现金流背离、债务压力等确定性规则；
- 每项异常显示公式、报告期、原始数值、数据来源和缺失状态；
- 不做盈利预测，不使用无依据的综合分数替代事实。

研究 Agent 必须是结构化研究流程，而不是聊天机器人：

- Intent Parser：识别用户问的是财报、估值、公告、行业还是风险；
- Task Planner：显示本次会检查哪些任务；
- Data Router：只调用已经接入的数据源，并暴露失败与覆盖边界；
- Analyzer：基于取得的数据做历史比较、同业比较或证据归类；
- Source Tracer：每个关键结论能够回到来源；
- Report Generator：输出“已确认 / 仍不确定 / 下一步验证”，不输出买卖建议。

### 3. 组合中心

组合中心包含普通持仓和 ETF 穿透，目标是回答“我真正暴露在哪里”，而不是只列资产。

- 支持手动和 CSV 导入，明确数据仅保存在何处；
- 展示单一持仓、行业、主题和底层重仓集中度；
- ETF 诊断展示披露日期、前十大持仓覆盖、重合股票、重合权重和数据边界；
- 将 ETF 与股票持仓合并看底层重复暴露；若数据暂不支持，明确标注而不是估算；
- 从风险项可直接创建一笔决策审查。

### 4. 决策审查

这是产品差异化核心，必须比其他模块更完整。

- 用户输入股票、操作、金额、理由和判断失效条件；
- AI 或本地解析器把理由拆成可验证事实、主观推断、未核实外部说法、信息来源、紧迫性表达和缺失项；
- 用户确认或修改结构化理解，模型不得静默决定；
- 普通代码计算操作前后仓位、个人规则冲突和 10%/20%/30% 下跌金额；
- RAG 只在有限公开来源中核实用户原话；未找到就明确写未找到；
- 最终审查卡按“数字影响 → 规则冲突 → 证据状态 → 未回答问题”排序；
- 用户只能选择维持、修改或稍后再看，系统不替用户放行或禁止；
- 保存原计划、最终计划、规则快照、证据快照、耗时和用户选择。

### 5. 交易复盘

- 支持真实 CSV 粘贴和上传；
- 按 FIFO 匹配买卖并计入费用；
- 展示已实现盈亏、未平仓数量、持仓成本、集中度和频繁交易等规则信号；
- 若没有最新市价，不计算未实现盈亏并明确原因；
- AI 报告只能解释确定性结果，不能新增不存在的数字；
- 复盘发现的问题可以写回个人规则或生成下一次决策审查提醒。

### 6. 历史与复核

- 记录原计划与最终选择的差异；
- 保存当时的证据、规则和数据时间，不能用今天的数据覆盖历史上下文；
- 支持匿名 CSV 导出；
- 行为报告只描述可观察行为，不进行心理诊断；
- 展示“哪些判断到了复核时间”，形成低交易频率之外的回访理由。

## 统一信息架构

一级导航最多六项：

1. 工作台
2. 研究
3. 组合
4. 决策审查
5. 交易复盘
6. 历史

ETF 诊断属于“组合”，财报体检和研究 Agent 属于“研究”，设置、隐私和语言放入用户菜单。不要把每个技术模块都变成一级导航。

## UI/UX 规范

### 产品感

- 桌面基准为 1440×900，最低支持 1100px；本轮不为手机重排。
- 整体像成熟研究工作台，而不是营销落地页、AI 对话记录或霓虹交易终端。
- 打开页面即进入任务，不使用巨型标题、口号和大面积空白。
- 高信息密度依靠清晰层级、对齐和分组，不依靠堆叠同质卡片。

### 视觉系统

- 使用中性浅色画布、深蓝主操作和少量琥珀/金色提醒；红绿只用于价格涨跌。
- 关键金额和比例使用等宽数字并优先于形容词。
- 主体正文不小于 14px，辅助信息不小于 12px；中文行高至少 1.5。
- 采用 4/8px 间距系统，控件高度 36–40px，圆角 8–12px。
- 使用 Lucide 图标，不使用 emoji 作为功能图标。
- 图表必须同时显示数值、图例、时间范围和文字摘要，不依赖悬停才能理解。

### 页面结构

- 应用栏 72px；一级导航 216–232px；内容区使用连续工作画布。
- 股票研究采用“标的列表 / 主研究区 / 下一步操作”三栏。
- 决策审查采用“主审查画布 / 证据与规则侧栏”，底部固定用户选择。
- 组合与复盘优先用表格、条带图、暴露矩阵和时间轴，不把每个数字做成 KPI 卡。
- 所有加载、空状态、失败和降级状态都给出下一步可操作入口。

### 交互与可访问性

- 所有按钮有 hover、active、focus-visible、disabled 和加载反馈。
- 键盘可完成搜索、切换标签、表单确认和主要决策。
- 对比度达到 WCAG AA；状态不能只靠颜色表达。
- 动画仅用于状态切换，160–220ms，并支持 prefers-reduced-motion。

## 数据与 AI 可靠性

- 所有数值带数据来源、更新时间和口径。
- 优先使用正式披露；媒体和社交说法永远不与正式披露混为一类。
- 在线数据失败时可以使用缓存；只有明确进入演示时才使用样例，并在页面显著标注。
- LLM 只做自然语言理解、证据归类和通俗解释；金额、比例、FIFO、阈值和异常规则由确定性代码计算。
- LLM 使用结构化 JSON Schema；输出必须经过字段校验和引用校验。
- 没有 AI Key 时，股票研究、财报规则、ETF 诊断、复盘计算和决策审查仍可运行。
- 不把 ChatGPT 或 Codex 订阅额度当作应用 API；真实模型必须通过服务器端 API 配置。

## 技术实施边界

- 现有 Next.js/shadcn 桌面前端作为唯一正式 UI。
- FastAPI 继续承载数据、ETF、财报、归因、审查和 AI 服务。
- 不使用 iframe 作为最终 ETF 或交易复盘体验；先复用已有 API，再用统一 React 组件重做页面。
- 业务逻辑保留在 Python service 层，不复制到页面组件。
- 不引入多智能体框架来制造复杂度；只有当任务拆解、来源跟踪和失败隔离确有价值时才增加 Agent orchestration。
- 不建立微服务、Redis、Kubernetes、多租户支付或券商连接。

## 合规与隐私

- 禁止“必买、必卖、看涨、目标价、胜率、保证收益、自动下单”等表达。
- 不收集券商密码、身份证、银行卡、短信验证码。
- API Key、数据 Token 和数据库凭据只在服务器端环境变量或 secrets 中。
- 免责声明集中放在结果和隐私说明的适当位置，不在每个输入框下重复产品自辩。
- 用户必须能删除本地持仓、规则和历史记录。

## 一个月范围

### P0：形成可交付闭环

1. 去除虚构实时数据和预填用户判断。
2. 统一工作台、研究、组合、决策审查、复盘、历史导航。
3. 用 React 原生页面接入现有 ETF 诊断与交易复盘 API。
4. 完成财报体检 API、规则、来源口径和桌面页面。
5. 完成结构化研究 Agent 的最小流程和来源追溯。
6. 持仓可导入，并进入工作台、ETF 穿透和决策计算。
7. 完成研究 → 决策 → 记录 → 到期复核闭环。
8. 无 AI Key 的所有核心流程可用。

### P1：课程高分证据

1. 20 个黄金案例、财报异常规则测试、ETF/复盘边界测试和 3 个幻觉红队案例。
2. 记录模型、提示词版本、来源、用户确认和最终选择。
3. 5 位目标用户测试和可导出数据。
4. 90 秒现场 Demo、30 秒备份视频和接口失败回退路径。
5. 免费/付费意愿实验，不实现支付系统。

### P2：有证据后再决定

- 定期变化提醒、同业深度比较、更广数据源、付费订阅、多用户账号。
- 小程序、App、完整 PWA、自动交易和全网社交抓取。

## 课程评分证据

每次实现都必须回答它增加了哪类证据：

- Problem：用户是否真的在交易前停下来检查？
- Innovation：结构化理由 + 可追溯证据 + 确定性风险 + 用户确认是否形成不同于通用 ChatGPT 的流程？
- AI-native：没有 LLM 时哪些功能仍可靠，有 LLM 时具体提升了什么？
- Feasibility：真实数据失败、缺字段和无 Key 时是否安全降级？
- Business：什么行为值得持续付费，而不是只生成一次报告？
- Responsible AI：是否能追溯、纠错、拒绝编造并保留人工决定？
- Demo：评委能否在 90 秒内看到输入、核实、数字变化和用户修改？

## 验收标准

只有同时满足以下条件才算完成：

- 任意合法 A 股代码或名称能够搜索；别名、空格、交易所后缀有容错。
- 财报体检至少使用 4 个真实报告期，并显示缺失与来源。
- 两只以上 ETF 能显示重合持仓、披露日期和数据覆盖。
- 真实交易 CSV 能返回 FIFO、费用、已实现盈亏、未平仓和规则信号。
- 用户粘贴一条外部说法后，可以看到正式披露、媒体和未证实信息的区别。
- 决策审查显示操作前后仓位和 10%/20%/30% 下跌金额。
- 用户能修正 AI 的理解，并保存维持、修改或延迟。
- 历史记录保留当时证据与规则快照，并可匿名导出。
- 无 AI Key、单一数据源失败、空持仓、空历史、错误 CSV 均有真实可用状态。
- 自动化测试、桌面烟雾测试、可访问性检查和 90 秒演示全部通过。
- 页面中没有开发提示、产品自我检视对话、静态样例冒充实时或直接买卖建议。

## 工作方法

每次只完成一个可验证纵向切片：先检查已有能力，再写验收，再实现 UI 与 API，最后用真实输入走通。不要主动增加清单外功能；不要用视觉润色掩盖假数据或断裂流程。任何模块若不能进入“研究 → 证据 → 个人影响 → 决策 → 复核”主线，就暂不扩展。
