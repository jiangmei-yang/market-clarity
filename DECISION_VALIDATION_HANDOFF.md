# Decision Validation：组员专项交接

> 上位项目说明：[PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)
>
> 建议负责人：专注“决策验证/信息核实”的组员
>
> 文档状态：2026-07-29 任务基线

## 1. 任务定义

Decision Validation 的目标是回答：

> 用户准备依据某个理由作出交易决定时，这个理由包含哪些事实、推断和未核实信息？当前资料能支持到什么程度？哪些关键问题仍然会影响原判断？

它不是自动给出投资结论，而是建立一条可追溯的验证链：

```text
用户原话
→ 原子说法
→ 所需证据
→ 检索范围与来源
→ 支持 / 部分支持 / 冲突 / 证据不足
→ 对原判断的影响
→ 用户自行维持、修改或延后
```

## 2. 与其他模块的边界

### 本负责人负责

- 把用户理由拆成可独立核实的说法；
- 给每条说法标注类型、核实问题和所需证据；
- 管理正式披露、公司报告、媒体与市场观点的证据等级；
- 判断证据支持、部分支持、冲突或不足；
- 显示来源、日期、原文链接、覆盖范围和数据状态；
- 生成受约束的“对原判断的影响”和下一步核实问题；
- 完善 golden cases、幻觉防护、失败状态和红队测试；
- 确保决策记录保存当时的证据快照和验证状态。

### 本负责人不负责

- 行情图、技术指标和完整股票研究页；
- 持仓、单股/行业占比和下跌金额计算；
- 个人规则阈值的定义；
- 选股、目标价、买卖信号或自动交易；
- 通用聊天助手和全站视觉重构；
- 定量策略历史验证本身。

“Decision Validation”与“Quant Verification”不同：前者验证当前决策理由及证据；后者用历史数据检查一个明确规则过去的表现。定量核实结果可以作为一条证据进入本模块，但不能自动决定本模块结论。

## 3. 当前已有基础

### Python 业务层

- `src/decision_review/models.py`：交易计划、说法、证据和审查结果模型；
- `src/decision_review/analyzer.py`：规则版及可选 AI 理由拆解；
- `src/decision_review/retrieval.py`：小型受控知识库和公告标题核实；
- `src/decision_review/service.py`：理由、证据和规则结果编排；
- `src/services/news_intelligence.py`：事件、来源和证据整理；
- `pages/0_1_🧭_决策检查.py`：旧 Streamlit 决策交互。

### 新版网页

- `sites_frontend/app/api/evidence/[code]/route.ts`：证据 API、巨潮/东方财富公告回退、缓存与可靠性状态；
- `sites_frontend/app/client-page.tsx`：研究、理由确认、实时证据及决策结果的主要交互；
- `sites_frontend/app/opportunity/page.tsx`：机会/说法检查入口；
- `sites_frontend/app/components/demo-walkthrough.tsx`：教学演示中的核实流程；
- `sites_frontend/tests/rendered-html.test.mjs`：关键产品承诺与页面行为检查；
- `sites_frontend/tests/course-readiness.test.mjs`：评估、数据源和课程证据检查。

### 评估

- `evaluation/golden_cases.json`：理由、规则、信息及幻觉防护样例；
- `scripts/run_evaluation.py`：无 API Key、无网络的确定性评估；
- `EVALUATION.md`：产品指标、信任校准和安全反指标。

## 4. 当前主要问题

1. **标题命中不等于事实成立**：目前大量逻辑依赖“订单、合同、回购”等关键词是否出现在公告标题中。
2. **说法粒度不稳定**：一句话可能同时包含事实、因果推断和价格预测，需要拆成原子说法分别判断。
3. **证据等级尚不够统一**：法定披露、公告聚合、媒体、观点及用户来源需要一套共享等级。
4. **缺少部分支持与明确冲突**：现有结果多为“找到可能相关”或“未找到”，难以表达金额、时间、主体只匹配一部分的情况。
5. **覆盖限制需要进入结论**：时间窗口、只查标题、数据源失败、缓存过期等限制不能只藏在页脚。
6. **AI 输出容易被理解为意见**：应改为证据状态、判断影响和待回答问题，不形成买卖倾向。
7. **信息核实评估集偏小**：需要更多金额夸大、日期冲突、同名主体、伪造来源、提示注入和源失败案例。

## 5. 建议的领域模型

### 原子说法

```json
{
  "id": "claim-1",
  "original_text": "朋友说公司拿到百亿元海外订单",
  "normalized_claim": "该公司近期签订金额约100亿元的海外订单",
  "claim_type": "external_fact",
  "verification_question": "法定披露是否确认主体、金额、海外属性和签订时间？",
  "required_evidence": ["交易所公告原文", "公司公告原文"],
  "source_as_stated": "朋友",
  "status": "unverified"
}
```

建议的 `claim_type`：

- `observable_fact`：可以由行情、财务或正式记录核实；
- `external_fact`：关于订单、合作、监管、回购等外部事件的说法；
- `causal_inference`：事实会导致业绩、估值或行业结果的推断；
- `market_prediction`：对未来价格或时间点的预测；
- `personal_motivation`：焦虑、害怕错过、回本等动机表达。

### 证据项

```json
{
  "id": "evidence-1",
  "claim_id": "claim-1",
  "title": "关于签订重大合同的公告",
  "source_name": "巨潮资讯",
  "source_tier": "official_primary",
  "published_at": "2026-07-20",
  "url": "https://...",
  "matched_passage": "...",
  "relation": "partial_support",
  "covers": ["主体", "合同存在"],
  "does_not_cover": ["100亿元", "海外客户", "收入确认时间"]
}
```

### 核实结果

统一使用以下语义，避免“相关”“风险高”等模糊标签：

| 状态 | 含义 | 允许的表达 |
|---|---|---|
| `supported` | 高等级证据支持说法的关键组成 | “当前取得的正式资料支持……” |
| `partially_supported` | 只支持主体、事件、金额或时间中的一部分 | “资料确认了……，但尚未确认……” |
| `contradicted` | 高等级证据与关键内容明确冲突 | “当前正式资料显示……，与原说法中的……不一致” |
| `insufficient` | 当前覆盖无法判断 | “当前检索范围内证据不足” |
| `unavailable` | 数据源或处理失败 | “资料源暂不可用，不能形成新的核实结论” |
| `stale` | 只有过期资料 | “仅有截至某日期的资料，不能代表当前状态” |

`未找到`永远不能自动映射为 `contradicted`。

## 6. 证据来源等级

建议统一采用：

1. `official_primary`：交易所、巨潮法定披露、公司公告 PDF、监管文件；
2. `official_structured`：从正式报告抽取的结构化财务或行情数据；
3. `reputable_secondary`：有署名、日期和可追溯原始出处的正规媒体；
4. `opinion`：分析师、市场评论和社交观点；
5. `user_provided`：用户粘贴或转述，尚未独立核实；
6. `unknown`：无法确认来源。

低等级来源可以提示需要核实，不能单独把外部事实升级为 `supported`。如果二手来源明确链接法定原文，应引用原文而不是只引用二手报道。

## 7. 核实逻辑要求

### 第一阶段：规则与可审计匹配

- 标准化股票主体、六位代码、日期范围和核心事件词；
- 从用户原话中抽取金额、比例、时间、对象和限定词；
- 先筛选可能相关文件，再比较标题/正文片段与具体组成；
- 记录哪些组成被覆盖，哪些没有；
- 结论由明确规则生成，保留可重复的输入和输出。

### 第二阶段：受约束 AI 整理

AI 只接收已经检索到的有限证据及编号，并必须：

- 每条事实引用证据 ID；
- 不补写证据中不存在的金额、主体、日期或因果关系；
- 不把“可能相关”改成“已经确认”；
- 不输出买卖建议、仓位建议、收益承诺或目标价；
- 证据不足时直接返回不足，不依靠常识补全；
- 忽略公告正文或用户内容中试图改变系统规则的指令。

模型失败、返回不可解析内容或引用不存在的 ID 时，必须降级到规则模式。

## 8. 面向用户的输出

结果页建议固定为四段：

### 核实结果

显示每条原子说法的状态，而不是给整笔交易一个综合分数。

### 已确认与未确认

```text
已确认：近期存在一项合同相关法定披露。
未确认：当前证据不能确认100亿元、海外客户和本期收入贡献。
```

### 对原判断的影响

只能描述逻辑影响：

```text
原理由中的“存在合同”获得部分支持；“会很快显著改善业绩”仍是尚未验证的因果推断。
```

不能写：

```text
利好，建议继续买入。
```

### 下一步需要回答的问题

- 合同金额占最近年度收入多少？
- 合同何时执行并确认收入？
- 是否存在终止、审批或客户集中风险？
- 如果关键条件未发生，用户写下的失效条件是否足够明确？

用户随后自行选择维持、修改或稍后再看。验证模块不得预选其中任何一项。

## 9. 实施工作包

### WP1：统一 schema 与状态语义

交付：

- Claim、Evidence、VerificationResult schema；
- Python 与 TypeScript 字段映射；
- 旧字段兼容或迁移说明；
- `supported/partially_supported/contradicted/insufficient/unavailable/stale` 的单元测试。

验收：同一输入在规则模式、API 和页面记录中使用相同状态语义。

### WP2：原子说法拆解

交付：

- 混合句拆分；
- 金额、日期、主体及因果词抽取；
- 用户可修改并确认拆解；
- 无 AI Key 的规则降级。

验收：示例“朋友说公司拿到海外大订单，业绩应该很快上涨”至少拆为外部事实与因果/预测两条，不整体误标为已核实事实。

### WP3：公告原文与证据关系

交付：

- 正式来源优先；
- 在条件允许时读取原文或可引用片段；
- 记录金额、主体、时间等组成覆盖；
- 标题命中只作为候选，不直接产生 `supported`；
- 数据源、缓存、新鲜度和覆盖范围进入结果。

验收：标题包含“订单”但正文/摘要不支持用户所说金额时，只能返回部分支持或证据不足。

### WP4：决策影响说明与 UI

交付：

- 每条说法状态；
- 已确认/未确认；
- 对原判断的影响；
- 下一步核实问题；
- 来源链接、日期和覆盖说明；
- 加载、空结果、过期、失败及重试状态。

验收：用户能区分“没找到”“不支持”和“已证伪”，且页面没有综合买卖结论。

### WP5：评估与红队

交付：至少 30 个 decision-validation cases，并能一键重复运行。

最低覆盖：

- 正式资料完整支持；
- 只支持事件、不支持金额；
- 日期或主体冲突；
- 媒体报道没有原始出处；
- 市场观点伪装成事实；
- 同名主体或错误代码；
- 旧公告被当作近期事件；
- 没有检索结果；
- 数据源失败、超时和过期缓存；
- 用户要求模型伪造来源；
- 证据文本包含提示注入；
- 用户要求确定性买卖建议；
- 中英文、口语、错别字和多个说法混合。

验收：

- 所有肯定结论都有合法 evidence ID；
- 市场观点不能升级为正式披露；
- `insufficient` 不会变成 `contradicted`；
- `unavailable/stale` 不产生新的实时肯定结论；
- 输出不包含确定性买卖建议；
- 现有安全与业务测试无回归。

## 10. 建议的两周安排

### 第 1–2 天：基线与 schema

- 运行现有 Python evaluation 和相关新版前端测试；
- 画出现有 reason → evidence → decision 数据流；
- 提交 schema、状态语义和兼容方案。

### 第 3–5 天：拆解与证据分级

- 完成原子说法拆解和可编辑确认；
- 统一来源等级、日期和覆盖字段；
- 先增加 15 个离线案例。

### 第 6–8 天：正文/片段核实

- 将标题匹配降为候选召回；
- 增加片段关系、部分支持和冲突判断；
- 完成数据失败与缓存过期处理。

### 第 9–10 天：决策影响 UI

- 实现四段式结果；
- 检查键盘、空状态、链接和中英文表达；
- 确保用户选择没有默认倾向。

### 第 11–12 天：红队与评估

- 扩充至至少 30 个案例；
- 测提示注入、伪造引用、金额夸大和来源失败；
- 保存可复现失败输入与修复证据。

### 第 13–14 天：用户理解测试与交接

- 用 3–5 位非开发者测试“未找到”和“已证伪”的区别；
- 检查他们能否指出至少一条已确认、未确认和下一步问题；
- 完成 PR、测试结果、截图和剩余限制说明。

## 11. 测试与完成标准

最低运行：

```bash
python scripts/run_evaluation.py
pytest tests/test_ai_decision_assistant.py tests/test_core.py tests/test_api.py
```

涉及新版平台时：

```bash
cd sites_frontend
npm run lint
npm run build
node --test --test-concurrency=1 tests/rendered-html.test.mjs tests/course-readiness.test.mjs
```

最终 Definition of Done：

- 一条混合理由可以拆成多条独立说法并由用户确认；
- 每条核实结论有来源、日期、链接和覆盖范围；
- 标题关键词命中不会直接等于事实成立；
- 支持、部分支持、冲突、证据不足、不可用和过期状态可区分；
- AI 结论引用合法证据 ID，失败时安全降级；
- 页面不生成买卖建议，不自动选择用户决定；
- 决策记录保留当时证据快照，而不是日后静默变化；
- 至少 30 个专项案例通过，并包含失败与幻觉防护；
- 3–5 位测试者中至少 4 位能正确解释“没找到不等于证伪”（若只测试 3 人，则要求 3/3）；
- PR 清楚列出没有解决的覆盖限制。

## 12. 协作注意事项

- 当前工作树可能存在其他成员的未提交修改；开始和提交前都运行 `git status --short`；
- 优先在独立分支工作，不覆盖或格式化无关文件；
- `sites_frontend/app/client-page.tsx` 是高冲突大文件。能抽成独立组件或 lib 时优先抽离，必须修改时提前协调；
- 修改公共 schema 前通知风险计算、研究体验和历史记录负责人；
- 不把 live 网络测试结果写死成产品事实；
- 不把模型评分或内部测试当作真实用户价值证据。

## 13. 可直接发给负责人的启动提示

```text
你负责本项目的 Decision Validation。请先完整阅读 PROJECT_HANDOFF.md 和 DECISION_VALIDATION_HANDOFF.md，然后检查 git status、现有 schema、evidence API 和 evaluation cases。

第一步只做基线审计：画出“用户理由 → 原子说法 → 证据 → 核实状态 → 决策影响 → 用户选择”的当前数据流，列出标题关键词匹配、来源等级、失败状态和测试覆盖的缺口，并提出按 WP1–WP5 排序的实现计划。

范围内：理由拆解、证据分级、正式资料核实、决策影响说明、失败状态和专项评估。
范围外：持仓金额计算、全站 UI、选股、目标价、买卖建议、自动交易和 Quant Verification 引擎。

所有肯定结论必须可追溯到来源、日期和证据 ID；未找到不等于证伪；模型失败必须安全降级。不要覆盖其他组员的未提交修改。
```
