# 安心看股 MVP 范围（冻结版）

## 唯一正式交付

当前课程 MVP 只验证一个产品切片：**面向不懂量化的普通 A 股投资者，用大白话生成、比较并保存可复用研究方法的 AI 策略研究室**。

它不推荐股票、不预测涨跌、不连接券商、不执行交易，也不进行心理诊断。研究层负责整理行情、财务、公告和新闻中的正反证据；决策层帮助用户在操作前把理由说清楚、核实信息缺口、计算金额影响，并对照自己预先确认的提醒边界再做决定。

课程 Module 8 口径下，首轮 MVP 实验只计算 `/pilot/strategy-lab` 的一条端到端行为：**大白话描述 → 确认规则 → 运行统一比较 → 看懂单个候选 → 保存研究方法**。交易前决策审查仍是产品的下游使用场景，但不进入这轮主指标；ETF、复盘与个性化工作台也不能用功能数量替代真实用户完成与复访证据。七字段证据契约见 `MVP_EVIDENCE_CONTRACT.md`，取舍见 ADR-0006。

受控发布默认失败关闭：只有 `MVP_PILOT_ENABLED=true` 才接收体验事件；生产环境必须配置 `NEXT_PUBLIC_BUILD_ID`，同一外部体验码只能绑定一个构建，发布新构建必须更换体验码。当前批次的 P0 安全事故由负责人使用独立 `MVP_PILOT_REVIEW_CODE` 复核；没有复核记录时安全门槛不通过。

## 本轮必须形成的闭环（P0）

1. 用自然语言描述个人提醒边界；
2. 系统转成结构化规则，用户逐项确认或修改；
3. 输入股票、操作、金额、理由和失效条件五项；
4. 系统把理由拆成事实、推断、未核实信息和情绪表达；
5. 用户确认系统理解；
6. 普通代码计算仓位、集中度和 10%/20%/30% 下跌情景；
7. 有限 RAG 返回可追溯资料及覆盖限制；
8. 生成不含买卖建议的决策审查卡；
9. 用户选择维持、修改或稍后再看；
10. 保存前后变化，并可导出匿名用户测试 CSV。
11. 研究页并列显示支持证据、反方证据和待核实信息；
12. 事件雷达区分正式披露、媒体报道与市场观点；
13. 研究背景可随股票和用户计划进入决策检查。
14. 单股或明确演示股票池的低频“定量核实”：自然语言生成候选规则、用户确认、确定性历史检验、统计审计及结果带入决策记录。

核心流程在没有 OpenAI API Key、实时行情或外网时仍可用固定演示数据完成。

## P1（只在 P0 稳定后）

- 扩充有限、可信、可维护的 RAG 资料；
- 完善资料日期和引用；
- 历史审查记录与简单行为报告；
- 在桌面真实用户测试后再决定是否恢复手机适配；
- 云部署、访问密码和数据备份说明。

## 暂不做（P2）

- 完整行情平台、自然语言选股和生产级全市场量化回测；当前只包含边界明确、不会生成买卖信号的低频定量核实；
- 买卖信号、自动交易、券商连接和全网新闻或社交平台抓取；
- 小程序正式发布、Android、iOS、Flutter、React Native；
- 完整 PWA、多端同步、多用户商业账户和支付；
- 复杂心理状态诊断或大规模生产架构。

## 架构边界

允许把规则解析、交易计划模型、规则计算、理由审查、有限检索、报告和研究记录从 Streamlit 页面分离；不做微服务、Kubernetes、Redis、多数据库抽象或跨端 UI 框架。

## 成功标准

- 新用户 2 分钟内完成个人规则设置；
- 60 秒内完成一次交易计划审查；
- 能纠正系统对原话的理解；
- 能看到金额风险和个人规则冲突；
- 能自行修改或延迟，并保留前后变化；
- 90 秒固定 Demo 无 Token、无实时接口也能完成；
- 桌面浏览器能完成研究到决策的完整流程；
- 能导出不含直接身份信息的测试数据。
