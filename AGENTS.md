# Codex 与开发协作规范

本文件适用于仓库根目录及其所有子目录。若以后增加更深层的 `AGENTS.md`，该文件可以为对应子目录补充更具体的要求。

## 开始工作前

1. 阅读 `PROJECT_CONTEXT.md`，了解产品目标、边界和当前架构。
2. 阅读 `TASKS.md`，确认任务负责人、范围、依赖和验收标准。
3. 涉及重大技术或产品取舍时，阅读 `docs/decisions/README.md` 及相关 ADR。
4. 涉及决策验证时，额外阅读 `DECISION_VALIDATION_HANDOFF.md`。
5. 运行 `git status --short`。仓库可能包含其他成员的未提交修改，不得覆盖、回滚、格式化或顺手提交无关文件。

## 产品不可突破的边界

- 不推荐股票，不输出确定性买入、卖出、目标价或收益承诺。
- 不连接券商，不自动执行交易。
- 不把媒体、市场观点、社交转述或关键词命中写成正式事实。
- “未找到证据”不等于“已经证伪”。
- 历史检验只描述历史样本，不解释为未来预测或买卖信号。
- AI 只用于受约束的语言理解、证据整理和白话解释；金额、仓位、风险边界和统计结果必须由确定性代码计算。
- 明确自伤危险表达必须停止股票分析并进入支持性分流；一般焦虑不能被做成心理诊断。

## 实现原则

- 正式桌面 Web 产品位于 `web/`；新增网页功能优先在这里实现，不得未经确认在 Streamlit 与 Web 前端重复实现同一功能。
- 可复用业务规则放入 `src/` 或已有服务层，不把核心规则散落在页面组件中。
- 修改共享 schema 或 API contract 前，先检查研究、决策记录、风险计算和评估消费者。
- 所有肯定性证据结论必须带来源、日期、链接或证据 ID，以及覆盖范围。
- 真实、缓存、演示、过期和不可用数据必须明确区分。
- AI 或自然语言生成的规则、配置和计划必须先让用户预览/确认，再产生持久影响。
- 尽量把 `web/app/client-page.tsx` 的新增逻辑抽到独立 component/lib，减少多人冲突。

## 文件与安全

- 不提交 API Key、Token、密码、真实证券账户、身份资料或个人金融明细。
- `.env.example` 只能包含占位值和说明。
- 不把秘密写入聊天、日志、截图、fixture、浏览器存储或测试输出。
- 不提交虚拟环境、依赖目录、构建产物、缓存或本地数据库。
- 不修改与当前任务无关的现有用户文件。

## 测试要求

按改动范围运行最小相关测试；合并前运行对应完整测试。

Python 基线：

```bash
pytest
```

新版网页基线：

```bash
cd web
npm run lint
npm run build
npm test
```

如果完整测试因环境或时间未运行，PR 必须明确列出已运行的测试、未运行项和原因。不得用“代码看起来正确”代替验证。

## 完成与交接

一次任务完成至少需要：

- 满足 `TASKS.md` 中对应验收条件；
- 没有突破产品、安全和隐私边界；
- 增加正常、空结果、失败、过期和越界输入测试；
- 更新受影响文档或 ADR；
- PR/交接说明包含问题、实现、测试、截图或样例、剩余限制；
- 不把内部模型评分或团队自测包装成真实用户证据。

## 重大决策记录

以下变化需要新增或更新 `docs/decisions/` 中的 ADR：

- 正式交付平台或主架构改变；
- 产品安全边界改变；
- 公共 schema、证据状态语义或数据源优先级改变；
- 引入新的外部服务、持久化方式或敏感数据处理；
- 会明显影响其他负责人范围的跨模块取舍。
