# 安心看股桌面版 UI/UX 重设计总提示词（旧版，保留参考）

> 2026-07-22：本文件已由更完整的 `MVP_90_POINT_PRODUCT_PROMPT.md` 取代。后续产品、效果图、Figma、开发和验收均以新文件为唯一主提示词，避免两份规范同时生效。

> 使用方式：这不是一次性“美化页面”的提示词，而是后续逐页设计、生成原型、实现和验收的唯一产品设计输入。任何实现开始前，先确认本文件；每次只完成一页并进行真实数据和浏览器验收。

## 1. 你的角色与任务

你是一名资深金融产品设计负责人、信息架构师、数据可视化设计师和前端设计工程师。请重新设计“安心看股”桌面网页版，使它从课程样板和同质化卡片面板，转变为一款普通 A 股投资者愿意实际打开、阅读和反复使用的研究与决策工具。允许并鼓励使用任务导向的金融仪表盘，但每一项可视化必须回答一个具体问题，不能用装饰图形或无依据评分制造“数据很多”的假象。

本轮只设计桌面网页，基准视口为 1440×900，最低支持 1100px 宽度。不要设计手机端、小程序、Android、PWA 或营销落地页。

产品不是荐股软件，不预测未来涨跌，不承诺收益，不连接券商，不执行交易。产品分为两层：

- Information Layer：真实行情、历史价格、公告、新闻、财报、ETF 持仓披露和交易记录；
- Decision Layer：用户的持仓、交易理由、证据缺口、规则冲突、仓位变化和下跌金额。

核心价值不是“AI 写了一份报告”，而是：

> 用户能快速看清今天发生了什么、哪些信息真正影响自己的持仓和原判断，以及一笔计划会带来什么数字后果。

## 2. 设计成功标准

重设计后必须满足：

1. 打开产品 5 秒内能看出当前数据是否真实、更新时间是什么、最值得处理的一件事是什么；
2. 10 秒内能找到任意股票搜索、持仓变化和新建决策入口；
3. 20 秒内能区分正式披露、媒体报道、市场说法和个人推断；
4. 不读长段落也能理解股票近期变化、证据覆盖和个人持仓影响；
5. 每页首屏只突出一个主要任务，最多一个主按钮；
6. 所有关键数字显示口径、时间和来源，不能把静态样例伪装成实时信息；
7. 用户可以从研究自然进入决策审查，也可以从决策返回对应研究证据；
8. 页面正文最小 14px，辅助文字最小 12px，核心数字 20–28px；
9. 当前上涨和下跌可用红绿，但任何普通提醒、错误、缺证或规则冲突都不能借用涨跌红绿；
10. 页面不出现开发对话、产品自我辩护、课程要求和重复免责声明。

## 3. 视觉方向

### 使用场景

一位普通投资者在开盘前、午间或收盘后，坐在明亮房间里的电脑前，用 10–30 分钟检查持仓变化、核实一条消息或审查一笔计划。他可能焦虑或担心错过机会，因此界面必须冷静、清楚、可信，不能用闪烁数字和强烈颜色进一步制造紧张。

### 视觉策略

- 主题：明亮桌面研究工作台；
- 色彩策略：克制底色 + 多个有职责的语义色，而不是“全页面蓝金”；
- 主色：深靛蓝/钴蓝，只用于主要操作、选择态和链接；
- 信息色：低饱和紫色用于“个人判断”，琥珀色用于“需要注意/证据不足”，青色用于“数据已核实”；
- 涨跌：红色和绿色只用于行情涨跌，并同时显示正负号；
- 背景：中性浅灰白，内容面板使用真实白色，不使用奶油色、玻璃拟态或装饰渐变；
- 阴影：只用于浮层和下拉菜单；主页面依靠分隔线、留白和背景层级；
- 圆角：控件 8px，主要面板 10–12px，不使用 24px 以上大圆角；
- 图标：统一使用 Lucide，图标旁必须有文字，不使用 emoji；
- 字体：中文使用 `Noto Sans SC / PingFang SC / Microsoft YaHei / system-ui`，金额、代码和比例使用等宽数字特性；不使用科幻展示字体。

参考的不是某个产品的皮肤，而是以下品质：

- Linear：导航和任务优先级清楚；
- Stripe Dashboard：数据状态、表格、来源和错误信息可信；
- Koyfin/专业研究终端：图表与事件共同服务于研究问题；
- Notion Database：信息可以逐层展开，不在首屏一次性倾倒。

## 4. 全局信息架构

主导航保持 8 个入口，但重新分组：

### 每日工作

1. 工作台
2. 股票研究
3. 决策审查
4. 我的持仓

### 分析工具

5. ETF 穿透
6. 交易复盘

### 个人记录

7. 历史记录
8. 我的规则

导航栏默认 72px 图标栏；鼠标聚焦或点击品牌可展开到 208px。图标下不放过小文字；折叠时使用可见 tooltip，展开时使用 14px 中文标签。当前页需要明显但克制的背景选择态，不使用粗侧边彩条。

### 全局顶部栏

每页统一包含：

- 左侧：当前页面名；
- 中间：全局股票搜索，可搜索代码、名称和行业；
- 右侧：市场状态、最近更新时间、刷新按钮、新建审查按钮；
- 市场状态示例：`已收盘 · 行情更新于 15:02:18`；
- 数据异常示例：`部分数据不可用 · 3 个来源中 2 个已返回`；
- 点击状态可打开数据来源抽屉，显示来源、更新时间、延迟口径和失败项。

禁止只显示一个含义模糊的“数据状态”按钮。禁止伪造实时跳动动画。只有数据真正刷新后，时间和数字才发生变化。

### 全局实时数据规则

每一个动态模块必须具备以下五种状态：

1. Loading：保留布局的骨架屏，不让内容跳动；
2. Live：显示来源和精确更新时间；
3. Delayed：显示延迟时间或最近交易日；
4. Partial：说明哪些字段成功、哪些字段缺失；
5. Unavailable：保留上一次缓存时明确标注时间；没有缓存就显示空状态，绝不替换为样例结论。

数据标签统一使用：`实时`、`延迟`、`部分可用`、`缓存`、`不可用`。不要使用“AI 已分析”“智能生成中”等含糊标签。

## 5. 全局内容和排版规则

### 页面阅读顺序

每页必须遵守：

```text
当前状态/变化
→ 对用户意味着什么
→ 数据和证据
→ 可执行的下一步
→ 方法与免责声明（下移）
```

不能从产品介绍、方法解释或大标题开始。

### 信息层级

- L1 页面标题：20–22px，仅一个；
- L2 任务/结论：18px；
- L3 模块标题：15–16px；
- 正文：14px，行高 1.55–1.7；
- 辅助文字：12px；
- 关键行情/金额：24–28px；
- 表格：13–14px，数字右对齐；
- 长段文字最多 3 行，超过内容折叠或转换为要点、表格、图表。

### 去 AI 味文案规则

界面只能描述用户、数据和任务，不能描述产品人格或开发过程。

禁止出现：

- “AI 一分钟总结”；
- “AI 帮你……”；
- “让 AI 为你……”；
- “我们不会替你决定”；
- “理性投资，从这里开始”；
- “发现机会，把握未来”；
- “开启智能投资之旅”；
- “研究一个具体问题”作为大标题；
- “系统没有用 AI 猜测填补”等开发解释反复出现在主内容；
- 长篇免责声明占据页面底部或首屏。

改用具体任务文案：

- `核实一条消息`；
- `这条说法有正式披露支持吗？`；
- `今天发生了什么`；
- `与上次判断相比`；
- `目前能确认`；
- `仍未确认`；
- `下一项需要关注`；
- `计划后单股占比 38%，超过你的 25% 上限`；
- `若再下跌 20%，该持仓账面变化约为 ¥18,000`。

免责声明只在首次进入、结果导出、AI 报告和“数据与方法”抽屉中完整出现；其他页面使用一行简短链接 `数据口径与使用边界`。

## 6. 页面一：工作台

### 页面必须回答

> 自从我上次查看以后，我的持仓和判断发生了什么变化？现在最值得处理哪一件事？

### 首屏结构

1. 顶部市场条：上证、深证、创业板的最新值、涨跌、更新时间和市场开闭状态；不是跑马灯；
2. 左侧主区域（约 68%）：`与你有关的变化`；
3. 右侧区域（约 32%）：`组合快照` 和 `待处理事项`；
4. 如果存在未完成的决策审查，在内容顶部显示一条紧凑任务条，而不是大 Hero。

### 与你有关的变化

按时间倒序展示最多 6 条，每条是一行可扫描记录，不做同质卡片：

- 股票名称与代码；
- 最新价和当日涨跌；
- 发生的事件：公告、财报、价格异常或用户粘贴的说法；
- 来源类别、发布时间、原文链接；
- 对原判断的影响：`支持`、`削弱`、`暂不能判断`；
- 是否与持仓有关；
- 主操作：`查看证据` 或 `进入审查`。

行内必须突出“变化”而不是重复股票背景介绍。例如：

`贵州茅台发布季度经营数据；经营现金流同比下降 12.4%，与你记录的“现金流保持稳定”存在偏差。`

### 组合快照

这里可以使用紧凑型仪表盘布局，但组件必须用于比较、阈值或变化。优先使用数字、趋势和水平暴露条，不使用没有目标值的圆形仪表：

- 已记录资产；
- 单一最大持仓及占比；
- 今日持仓估算变化；
- 超出个人规则的持仓数量；
- 行业暴露前 3 项；
- 数据覆盖：`6/8 只持仓取得最新行情`。

### 待处理事项

只显示最多 3 条：

- 外部说法尚未核实；
- 判断到达复核日期；
- 计划超过个人仓位上限；
- 数据源连续失败。

每条必须包含明确动作，不能只写“高风险”。

### 空状态

没有持仓时，不展示虚构行情案例。显示：

`添加一只持仓或关注股票后，这里会显示与你有关的公告、价格变化和判断复核。`

主按钮：`添加持仓`；次按钮：`先研究一只股票`。

### 验收

- 首屏没有大标题和口号；
- 用户能立即看到更新时间和至少一个真实变化；
- 没有持仓时不展示与用户无关的“今日重点”；
- 同一事件不在三个模块重复出现。

## 7. 页面二：股票研究

### 页面必须回答

> 这只股票最近发生了什么？哪些是事实，哪些仍然只是说法？什么信息会改变我的判断？

### 页面布局

- 左侧 240px 股票列表：关注、持仓、最近查看；
- 中间主研究区：最少 700px；
- 右侧 300px 决策栏；
- 股票列表不是主视觉，不能占用过多宽度；
- 用户选股后只刷新研究内容，不改变整个页面结构。

### 股票头部

同一行显示：

- 名称、代码、市场、行业；
- 最新价、涨跌额、涨跌幅；
- 成交额；
- 行情时间；
- 数据来源状态；
- 关注按钮；
- `核实消息` 输入框作为次级操作，不与行情争夺视觉。

如果最新价来自最近交易日，必须写 `最近交易日 7月21日收盘`，不能写“实时”。

### 首屏核心：今天发生了什么

股票头部下方首先显示 3–5 条变化，不先展示抽象总结：

- 价格：近 1 日/5 日/20 日变化及是否出现异常成交；
- 正式披露：最新公告或财报中的具体变化；
- 媒体/市场：与用户输入说法相关的公开报道；
- 与持仓相关：该变化对应的金额暴露；
- 资料缺口：当前无法核实的关键点。

每条必须有日期、来源和影响标签。

### 价格与事件图

使用一张“价格—成交量—事件”联动主图，这是股票研究页最重要的可视化，不同时堆放多个没有联系的小图：

- 默认 3 个月；支持 1月、3月、6月、1年；
- 上层为价格折线或 K 线，下层为共享时间轴的成交量；
- 公告、财报、监管问询、业绩预告和用户正在核实的新闻以不同形状的事件锚点标在时间轴上；
- 正式披露使用实心标记，媒体报道使用空心标记，用户粘贴的说法使用虚线标记，不能只靠颜色区分；
- 同一天事件过多时聚合为事件簇，点击后展开，不让标签互相遮挡；
- hover 显示日期、价格、涨跌、成交量、事件标题和来源；
- 点击某个事件后，将图表聚焦到事件前 5 个交易日至后 10 个交易日，并在右侧打开事件详情；
- 事件详情显示 `当日涨跌`、`后 1 日/5 日涨跌`、`成交量相对 20 日均值`、`相对沪深300或行业指数的区间差值`；
- 上述数据只能称为“事件前后价格反应”或“同期变化”，不能写成“事件导致上涨/下跌”；
- 允许用户切换基准：无基准、沪深300、所属行业指数；
- 允许按正式披露、财报、媒体、用户说法筛选事件；
- 图表旁显示当前选中事件的证据关系：支持原判断、削弱原判断、暂不能判断；
- 图下显示区间涨跌、最高/最低和最大回撤；
- 无 OHLC 数据时使用折线，不伪造 K 线；
- 必须提供可展开的数据表作为无障碍替代。

这张图必须帮助用户回答：

1. 价格或成交量的显著变化发生在事件之前还是之后？
2. 同期大盘或行业是否也发生了类似变化？
3. 市场反应是短暂的还是持续了多个交易日？
4. 该事件是否真正涉及用户正在核实的判断？

不得只在 K 线上放几个无法点击的小圆点，也不得把时间上的相邻关系写成因果关系。

### 研究结论区

不用一段“AI 总结”。改为三栏证据账本：

1. `目前能确认`：来自行情、财报或正式披露的事实；
2. `仍未确认`：用户说法、媒体推断或缺少原文的数据；
3. `下一项需要关注`：明确指标、阈值和时间。

每一项包含来源和日期。没有证据时显示 `本次检索未找到直接支持`，不能用相邻新闻替代。

### 财报体检 Tab

先显示最新报告期和同比口径，再显示：

- 营收、归母净利润、经营现金流、应收账款、存货、资产负债率；
- 本期值、同比值和方向；
- `利润—现金流`、`营收—应收`、`收入—存货`、`负债—现金流`四项勾稽；
- 一张 6–8 季度趋势图；
- 异常项展开后显示计算公式、原始字段和来源；
- 同行业比较未接入时显示明确空状态，不显示“无法比较”的假分数。

### 证据链 Tab

使用证据表而不是卡片墙：

| 时间 | 类型 | 结论/标题 | 来源 | 与问题的关系 | 状态 |

类型至少区分：正式披露、财报数据、行情、媒体、用户说法。支持按来源和关系筛选。

### 右侧决策栏

显示：

- 当前持仓金额和占记录资产比例；
- 用户当前判断；
- 判断失效条件；
- 距下次复核日期；
- 买入、补仓、卖出、继续观察；
- 唯一主按钮：`带着当前资料进入审查`。

没有持仓时不能开放“补仓”和“卖出”。

### 验收

- 首屏能同时看到价格、更新时间、最近变化和一张图；
- 真实数据和静态研究模板不能混在一起；
- 不使用 0–100 的“市场情绪分”“综合评分”制造权威感；
- 用户无需读超过三行的摘要即可理解重点；
- 任意证据可以点击回到来源。

## 8. 页面三：新建决策审查

### 页面必须回答

> 我准备做什么？为什么现在做？什么情况说明我的判断可能错了？

### 页面布局

- 左侧 62%：交易计划输入；
- 右侧 38%：随着输入实时变化的计划影响；
- 不使用五个同权重输入框一次性压给用户；
- 使用三段渐进流程，但保持在一个页面，不做冗长向导。

### 第一段：计划

- 股票；
- 操作：买入/补仓/卖出/继续观察；
- 计划金额；
- 当前持仓自动带入；
- 若从研究页进入，自动带入股票、研究问题和已核实证据。

右侧即时显示：

- 当前金额 → 计划后金额；
- 当前占比 → 计划后占比；
- 个人规则上限；
- 差值；
- 10%/20%/30% 下跌情景金额。

使用 bullet chart 和水平金额条。圆形仪表只允许展示存在明确上限或目标的单一指标；仓位前后差值这类需要精确比较的数据仍使用 bullet chart，不能为了“像仪表盘”牺牲可读性。

### 第二段：理由

让用户用一句或几句话说明理由。页面在输入后显示四类结构化内容：

- 可核实事实；
- 个人推断；
- 未核实外部说法；
- 紧迫性/情绪表达。

不得把“朋友说”“新闻很多”自动当成事实。每个提取项允许用户直接修改分类或删除。不要将底层 JSON 直接展示给普通用户。

### 第三段：失效条件与期限

- 什么情况说明判断可能错了；
- 预计观察/持有期限；
- 下一次复核时间；
- 对外部说法执行核实。

如果用户不知道怎么写，提供具体句式：

`如果下一期经营现金流没有改善，或订单未在正式披露中出现，我会重新检查这个判断。`

不要使用投资建议式默认答案。

### 右侧实时反馈

固定显示三个区域：

1. `数字影响`；
2. `规则差值`；
3. `资料完成度`。

按钮状态必须解释原因：

- 可提交：`生成审查结果`；
- 缺失：`还需填写失效条件`；
- 待核实：`先核实 1 条外部说法`；
- 用户始终可以选择 `保存并稍后继续`。

### 验收

- 用户不需要理解 JSON 或 RAG；
- 填金额后 300ms 内看到仓位和情景变化；
- 不允许在理由、失效条件或必要核实缺失时提交最终维持/修改；
- 页面不存在产品自我说明和课程术语。

## 9. 页面四：决策审查结果

### 页面必须回答

> 这笔计划触发了什么具体问题？差多少？哪些理由有证据，哪些没有？我准备如何处理？

### 首屏顺序

1. 一句事实结论：`本次计划触发 2 项个人规则，1 条外部说法尚未核实`；
2. 三个核心可视化；
3. 问题清单；
4. 用户选择。

不要输出“建议买入”“谨慎持有”“风险评分 72”等代替用户选择的结论。

### 三个核心可视化

1. 仓位前后对比：当前 19.6% → 计划后 38.0%，规则线 25%；
2. 情景损失：下跌 10%/20%/30% 对应的金额，金额标签始终可见；
3. 理由证据矩阵：事实、推断、未核实信息、失效条件的完成数量。

### 问题清单

使用表格：

| 检查项 | 当前计划 | 个人规则/证据要求 | 差值 | 如何处理 |

示例：

`单股仓位 | 38% | 上限 25% | +13 个百分点 | 修改金额或保留并记录原因`

### 理由与证据

左侧展示用户原话；右侧逐句对应事实、推断和来源。来源可点击。找不到证据时写：

`截至 14:32，本次检索的 2 条正式披露和 3 条媒体资料中未找到“大订单”直接证据。`

不要写笼统的“可信度较低”。

### 底部固定操作栏

- `维持原计划`；
- `修改计划`；
- `稍后再看`；
- 任何选择都由用户点击；
- 维持或修改时记录理由、最终金额、时间和下一次复核点；
- 修改金额后，三张图和问题清单即时更新，并显示“修改前/修改后”。

### 验收

- 结果先给数字差值，不先给形容词；
- 任何颜色状态都有图标和文字；
- 不出现推荐和预测语言；
- 用户能在 30 秒内说出触发了哪条规则、可能损失多少钱、哪条消息没被证实。

## 10. 页面五：我的持仓

### 页面必须回答

> 我的资金主要暴露在哪里？哪些持仓超过了自己的边界？哪些股票今天有重要变化？

### 首屏

- 记录资产总额；
- 持仓覆盖率；
- 最大单股占比；
- 最大行业暴露；
- 今日估算变化及更新时间；
- 一张水平持仓分布图，显示个人上限线；
- 不使用大面积环形图。

### 持仓表

| 股票 | 最新价/时间 | 持仓金额 | 资产占比 | 今日变化 | 个人边界 | 新事件 | 操作 |

操作：研究、审查、修改持仓。超过边界时显示具体差值，不只显示“超限”。

### 行业暴露

使用排序后的水平条形图，最多 10 个行业，显示金额和比例。行业数据缺失时展示覆盖率，不能把未知行业归入虚构分类。

### 数据导入导出

- 支持 CSV 导入、手工编辑和 CSV 备份；
- 导入前展示预览和字段错误；
- 不上传券商密码、身份证、银行卡或短信验证码；
- 页面说明收进数据与方法抽屉，不占据主内容。

### 验收

- 用户能一眼找到最大暴露和超限持仓；
- 所有市值口径标明是否使用最新价或用户手工金额；
- 没有持仓时给出明确添加路径，不展示样例持仓。

## 11. 页面六：ETF 穿透

### 页面必须回答

> 我买的多只 ETF 底层是否重复？真实暴露集中在哪些股票和主题？披露数据有多旧？

### 页面布局

- 左侧 32%：ETF 搜索和组合输入；
- 右侧 68%：诊断结果；
- 选择列表和诊断结果保持同时可见；
- 最多添加 10 只 ETF。

### 结果首屏

- ETF 数量；
- 已取得底层股票数量；
- 持仓披露覆盖率；
- 最近和最旧披露日期；
- 重复股票数量；
- 一句话具体结论，例如：`3 只 ETF 的已披露前十大持仓中，7 只股票重复出现，其中中芯国际合计披露权重最高。`

### 可视化

1. 主题/行业暴露水平条；
2. ETF × 重复股票矩阵，单元格显示披露权重；
3. 重复股票排序表；
4. 每只 ETF 的披露日期和数据覆盖表。

不要用复杂桑基图掩盖不完整数据。只有在持仓覆盖足够且关系明确时才允许提供桑基图作为次级视图。

### 数据边界

醒目标出：`持仓来自截至 YYYY-MM-DD 的定期披露，不代表当前实时持仓。`

没有持仓披露时不得用演示持仓替代。

### 验收

- 用户能在 20 秒内识别最重合的股票；
- 所有结果均显示披露日期；
- 名称推断的主题和真实持仓计算的暴露必须分开标记。

## 12. 页面七：交易复盘

### 页面必须回答

> 我的盈亏来自哪些已完成交易？当前还有哪些未平仓头寸？交易记录暴露了什么可核实的行为模式？

### 输入区

- CSV 上传、粘贴、示例格式下载；
- 上传后先显示前 10 行预览和字段映射；
- 明确列出被接受、被忽略和出错的行数；
- 只有用户确认后才运行归因。

### 结果首屏

- 总买入金额；
- 总卖出金额；
- 已实现盈亏；
- 手续费；
- 未平仓标的数量；
- 计算口径：FIFO；
- 数据覆盖日期。

### 可视化和表格

1. 已实现盈亏按股票的排序条形图；
2. 费用与交易现金流的简洁 waterfall，仅在数据完整时展示；
3. 未平仓头寸表；
4. 交易时间线；
5. 规则触发项：单股集中、交易过于频繁、亏损后短时间补仓、字段缺失。

规则只描述记录中发生的事实，不进行心理诊断。例如写：

`同一股票在亏损卖出后 4 小时内重新买入，触发你设置的 24 小时冷静期。`

不要写：`你存在报复性交易心理。`

### AI 报告

AI 自然语言报告作为次级 Tab，必须在确定性结果之后。报告只能归纳页面已计算的数据，并显示模型、生成时间和免责声明；不能新增页面中没有的数据或买卖建议。

### 验收

- 不配置 AI Key 时全部归因和规则检查仍可运行；
- 用户能够追溯每笔 FIFO 匹配；
- 结果区不把未实现盈亏伪装成已实现盈亏。

## 13. 页面八：历史记录

### 页面必须回答

> 我过去审查了哪些计划？原计划和最终选择有什么变化？哪些判断到了复核时间？

### 页面主体

以可筛选表格为主，不用卡片时间线作为主视图：

| 时间 | 股票 | 操作 | 原金额 | 最终金额 | 选择 | 触发规则 | 未核实信息 | 下次复核 |

支持筛选：股票、操作、最终选择、日期、是否修改、是否到期。

点击一行打开侧边详情，不使用阻断式模态框。详情包含：

- 用户原始理由；
- 结构化理解；
- 当时证据；
- 原计划与最终计划；
- 当时规则快照；
- 后续新增资料；
- 用户复核记录。

### 行为概览

顶部只显示 3 个可验证指标：

- 完成审查次数；
- 修改或延迟比例；
- 最常触发的规则。

不要输出“理性指数”“冲动分”“投资人格”等心理标签。

### 导出

支持匿名 CSV，至少包括测试者编号、完成时间、触发规则、原金额、最终金额、最终选择和复用意愿；不包含姓名、券商账号或直接身份信息。

### 验收

- 用户能快速找到修改前后的差异；
- 历史记录不是只读墓地，每条到期判断都有 `重新核实` 操作；
- 页面没有未经数据支持的行为评价。

## 14. 页面九：我的规则

### 页面必须回答

> 我希望系统在什么具体情况下提醒我重新检查？当前规则会如何影响下一笔计划？

### 首次设置

先提供三种入口，不称为风险测评：

- 强提醒模板；
- 标准提醒模板；
- 用一句话自定义。

自然语言示例：

`我大约用 20 万元投资，单只股票不超过 5 万元；亏损后准备补仓时，希望隔一天再看。`

解析后展示逐项确认表：

| 你的原话 | 系统理解 | 当前值 | 是否明确 | 修改 |

用户必须确认后保存。模型不能自行决定“适合”的比例。

### 已设置规则

用一张清楚的规则表，不用多个小卡片：

| 规则 | 当前值 | 最近触发次数 | 对计划的影响 | 操作 |

规则包括：记录资金、单股金额上限、单股比例上限、单笔提醒金额、冷静期、是否要求失效条件。

### 影响预览

用户修改规则时，右侧即时预览：

- 当前哪些持仓会超过新边界；
- 最近哪些历史计划会被触发；
- 不把模板描述成投资建议。

### 验收

- 新用户 2 分钟内能完成；
- 页面不要求先理解“集中度”等术语；
- 所有保存都显示明确成功状态；
- 修改规则前后有差异确认。

## 15. 共用组件与图表规范

### 必须建立的共用组件

- `DataFreshness`：来源、时间、实时/延迟/部分可用；
- `SourceBadge`：正式披露、财报、行情、媒体、用户说法；
- `EvidenceRelation`：支持、削弱、未确认；
- `MetricDelta`：当前值、历史值、差值、口径；
- `ThresholdBar`：当前值、计划值、规则线；
- `ScenarioLossBars`：10%/20%/30% 金额情景；
- `EvidenceLedger`：事实、推断、未核实信息；
- `SourceDrawer`：所有数据来源与失败状态；
- `InlineError`：错误发生位置、原因、可重试操作；
- `EmptyState`：说明为什么为空及下一步；
- `Skeleton`：与最终布局等高，避免跳动。

### 图表规则

- 仪表盘是页面组织方式，不是问题；问题是无目标、无比较、无时间和无来源的装饰图。工作台、研究页、持仓页可以使用任务导向仪表盘；
- 每张图在设计前先写出它回答的唯一问题，例如“哪只持仓超过个人上限”或“公告前后是否出现异于大盘的价格与成交变化”；
- 时间趋势使用折线/K线，不用面积渐变装饰；
- 事件与价格关系使用共享时间轴的价格、成交量和可交互事件锚点，并提供事件窗口对比；
- 分类比较使用排序水平条；
- 阈值比较使用 bullet chart；
- 仓位不要默认使用饼图；
- gauge 只用于存在明确上限、目标或安全区间的单一指标；没有目标值时改用数字、趋势或 bullet chart；
- 不使用雷达图展示不同单位指标；
- 每张图必须有标题、时间范围、单位、来源、更新时间和文本结论；
- 不依赖 hover 才能读取核心数值；
- 所有图表提供表格或文字替代；
- 图表加载失败不能让整个页面空白。

## 16. 页面状态与错误文案

### Loading

`正在读取最近交易日行情和公开披露…`

### Partial

`行情和历史价格已返回；正式披露暂未返回。你仍可查看价格，但不能据此确认外部说法。`

### Unavailable

`公开行情服务暂时没有响应。上次成功更新：7月21日 15:02。`

操作：`重试`、`查看来源状态`。

### No evidence

`本次检索未找到与“大订单”直接相关的正式披露。相邻行业新闻未被当作该说法的证据。`

### Empty portfolio

`还没有持仓记录。添加后，工作台才能计算仓位、金额影响和与你有关的变化。`

文案必须具体说明已完成什么、缺少什么、用户还能做什么。禁止只写“出错了”“风险较高”“数据不足”。

## 17. 交互动效

- hover、选择、展开：150–220ms；
- 数值刷新时只做轻微背景提示，不做滚动数字和闪烁；
- 图表时间范围切换保持空间连续；
- 页面加载不做编排式入场动画；
- `prefers-reduced-motion` 下移除位移，只保留即时状态变化；
- 所有按钮具备 hover、focus-visible、active、disabled、loading；
- 键盘可完成导航、搜索、表单和结果选择。

## 18. 技术实现约束

- 保留 Next.js/vinext、React、TypeScript、shadcn 和 Lucide；
- 初始数据优先在服务端读取；慢数据使用独立 Suspense/骨架边界；
- 实时/定时刷新不得导致整页重新渲染；
- 动态区域预留尺寸，避免 CLS；
- 业务计算不得藏在视觉组件里；
- 现有 API 失败时必须显示真实失败状态；
- 禁止为了展示效果硬编码股票价格、公告结论和更新时间；
- 本轮只实现桌面端，不为移动端牺牲桌面信息架构。

## 19. 实施顺序

不要同时重写全部页面。严格按以下顺序：

1. 全局 Shell、字体、色彩、数据状态组件；
2. 工作台；
3. 股票研究；
4. 新建决策；
5. 决策结果；
6. 我的持仓；
7. ETF 穿透；
8. 交易复盘；
9. 历史记录；
10. 我的规则。

每一页执行：

```text
确认该页唯一问题
→ 生成高保真桌面原型图
→ 用户确认方向
→ 实现真实状态
→ 浏览器逐项验收
→ 空/加载/部分失败验收
→ 再进入下一页
```

## 20. 每页交付物

每次只交付当前页面的：

1. 1440×900 高保真原型；
2. 信息层级说明；
3. 页面数据字段清单；
4. 默认、空、加载、部分可用、错误五种状态；
5. 交互说明；
6. 可访问性检查；
7. 实际代码；
8. 浏览器截图；
9. 自动化测试；
10. 与本提示词逐项对照的验收报告。

## 21. 最终拒绝清单

如果设计出现以下任意一项，应退回重做：

- 首屏是大标题、口号或产品自夸；
- 9–11px 大量正文；
- 十几个相同卡片平均铺开；
- 把长段 AI 摘要作为页面重点；
- 静态样例价格与真实行情混排；
- 没有更新时间和来源；
- 蓝金渐变、玻璃拟态、霓虹终端或巨大圆角；
- 无意义的综合评分、情绪仪表盘和买卖信号；
- 将“朋友说”“新闻很多”标记为事实；
- 信息不足时仍生成肯定结论；
- 图表只在 hover 时显示关键值；
- 页面重复解释产品理念和免责声明；
- 用“AI”“智能”“赋能”代替具体功能；
- 每个页面都使用同一种三卡片布局；
- 为了显得丰富而加入与用户任务无关的信息。

## 22. 本轮设计确认点

在写代码前，先确认以下方向：

- 桌面端、高保真、最终可上线，而不是概念草图；
- 先做全局 Shell 和工作台，再做股票研究；
- 首屏展示真实变化与个人持仓影响，不展示口号；
- 使用克制、明亮的研究工作台风格，不再限制为单一蓝金；
- 通过图表、表格、来源和时间增强可信度，而不是增加 AI 生成文字；
- 所有动态内容必须有真实状态和失败状态。

确认后，再进入第一阶段：为“全局 Shell + 工作台”生成 2–3 个不同信息拓扑的高保真原型方向；此时仍不修改代码。
