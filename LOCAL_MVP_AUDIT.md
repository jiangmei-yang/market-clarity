# 本地 MVP 缺口盘点

状态日期：2026-07-21
当前范围：只完善 Streamlit 网页版交易前决策审查，不继续部署、小程序、Android 或完整 PWA。

## 本轮已经修复

- 修复六位股票代码被误识别成计划金额的问题；
- 将“可接受亏损”改为确定性的 20% 下跌金额检查，不再要求用户每天重复填写一个隐藏字段；
- 行业资料缺失时停止错误的行业合并计算，并明确显示“行业集中度待补充”；
- AKShare 个股资料可用时读取股票简称和行业；
- 从首页开始新计划时清除上一笔审查状态；
- 结果页增加“开始新的检查”；
- 生成结果期间显示明确的加载状态；
- 删除页面中的“语音原型占位”开发文案；
- 自动化测试由 61 项增加到 81 项并全部通过；
- 已按设计基准落地桌面首页、交易前检查、个人规则、历史记录、持仓和股票分析入口；
- 使用独立空白数据库完成“设置规则 → 演示检查 → 修改金额 → 历史回看”的实际桌面浏览器验收；
- 使用贵州茅台、宁德时代和招商银行完成真实报价、公司资料、历史行情、公告和新闻烟雾测试；
- 修复结果页生成后仍重复显示计划输入表单的问题；
- 区分 daily_stock_analysis 的“可用 / 待配置 / 未连接”状态，并更新到当前 Streamlit iframe 接口。
- 增加支持证据、反方证据和待核实信息并列的研究快照；
- 增加带来源分级的事件与观点雷达，并打通研究到决策结果的上下文传递；
- 自然语言中的“准备持有一年”等期限会自动进入计划字段，不再被当成公司事实。

## P0：桌面版完成情况

1. 全新本地数据库端到端浏览器验收：完成；
2. 3 只真实 A 股行情、行业、公告和新闻烟雾测试：完成；
3. 无 AI Key 的本地规则流程与固定演示：自动化及浏览器验收完成；
4. 空持仓状态、CSV 备份与交易前检查跳转：完成；
5. 固定课堂演示与空白临时测试环境：完成；
6. 桌面核心页面视觉走查：完成，详见 `DESKTOP_MVP_ACCEPTANCE.md`。

手机端已经按用户指示暂停，不再作为本轮桌面 MVP 的完成条件；旧的手机验收结论不得继续引用。

## P1：完成 P0 后优化

1. 补齐所有次级页面的中英文翻译，避免同一页中英文混用；
2. 将匿名测试记录拆成阶段时间，区分规则设置耗时和单次审查耗时；
3. 为决策记录提供单条 JSON/CSV 备份与恢复，而不只提供汇总导出；
4. 继续统一少数旧页面的数据状态标签；
5. 在获得第一轮真实用户反馈后，只修复阻碍任务完成的高频问题。

## P2：继续暂停

- 原生 App、小程序正式发布、完整 PWA；
- 自动选股、量化回测、买卖信号和自动交易；
- 全网社交媒体抓取、复杂情绪诊断；
- 支付、多用户账户和大规模基础设施。

## MVP 工程完成标准

桌面版 P0 已通过，可交给小范围真实用户测试。真实用户证据、留存和付费意愿属于下一阶段，不用代码完成度代替。
