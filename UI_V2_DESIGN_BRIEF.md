# 安心看股 UI V2：第一阶段设计简报

## 当前唯一目标

先把“交易前检查结果页”做成一个普通用户愿意继续操作、能在 10 秒内读懂重点的产品页面。暂不重做全部页面。

2026-07-21 范围调整：当前只优化桌面网页版。已有手机样式保留，但暂停继续设计、适配和验收。

## 模拟用户的主要摩擦

1. 当前流程有明显的连续填表感，缺少持续可见的计划上下文；
2. 结果页模块很多，但视觉权重接近，最关键的数字没有形成第一眼焦点；
3. 公开信息很有用，但会把“维持 / 修改 / 稍后再看”推到页面下方；
4. 解释文字、标准控件和结果内容混排，仍像课程原型；
5. 缺少一个清楚的“检查前 → 检查后”价值时刻。

## 借鉴方法

- Fiscal.ai：单页快照、统一搜索、最近发生什么、关键数据优先；
- TradingView：自然语言转换后必须展示结构化解释；
- daily_stock_analysis：稳定侧栏、历史对象、中央主结论、状态和后续动作；
- Claude 风格 UI：统一设计变量、严格栅格、层级明确、状态完整、文案克制。

## 结果页内容顺序

1. 紧凑的股票与计划上下文；
2. “需要核对 3 项”状态，不给买卖建议或虚假综合评分；
3. 三个关键发现：单股仓位、20% 下跌金额、未核实信息；
4. 只显示最相关的 1–3 条公告或新闻；
5. 固定可见的三个动作：保持计划、修改金额、稍后再看；
6. 详细市场指标、完整证据和规则放入次级区域。

## 视觉原则

- 明亮浅蓝背景、白色内容面板、深海军蓝正文、少量金色强调；
- 红色和绿色只表示真实市场涨跌，不用于普通按钮或品牌装饰；
- 不使用大口号、大段 AI 自述、霓虹效果或虚假智能评分；
- 卡片数量受控，用留白、字号和位置建立层级；
- 桌面优先，但结构必须能自然折叠为手机单栏。

## 已确认的设计基准

- 桌面端：`docs/design/anshin-decision-review-v2.png`
- 390px 手机端：`docs/design/anshin-decision-review-mobile-v2.png`
- 桌面首页：`docs/design/anshin-home-desktop-v2.png`
- 桌面个人规则：`docs/design/anshin-rules-desktop-v2.png`

## 桌面落地状态

- 首页：完成；
- 交易前检查：完成；
- 个人规则：完成；
- 历史记录：完成；
- 我的持仓：完成；
- 股票分析统一入口：完成。

实际浏览器验收和数据证据见 `DESKTOP_MVP_ACCEPTANCE.md`。

## 桌面首页落地约束

1. 首屏是工作台，不是营销落地页；不使用大标题和口号。
2. 用户可在首页选择买入、补仓或卖出，并直接输入一句交易计划。
3. 最近检查、股票资料、持仓、个人规则和完成记录必须在第一屏可见。
4. 最近检查显示具体待核对项，不使用泛化的 AI 总结。
5. 股票资料是信息层入口，交易前检查是决策层主入口，两者不混为“AI 选股”。

## 手机端落地约束

1. 不得把桌面端三列卡片简单缩小；三项关键数字改为可扫读的纵向行。
2. 顶部同时保留产品身份、股票名称、代码、最近价格和当日涨跌。
3. 红绿只能出现在当日涨跌数字；事实、风险和动作使用蓝色或金色。
4. “保持计划 / 修改金额 / 稍后再看”始终位于底部导航上方，三个按钮不折行为纵向列表。
5. 手机底部导航使用“首页 / 分析 / 持仓 / 决策 / 我的”，当前页必须有明确激活状态。
6. 首屏不出现英文口号、AI 评分、买卖建议或对话气泡。
