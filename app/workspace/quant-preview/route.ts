import {NextResponse} from "next/server";import {previewQuantWorkspace,saveNewQuantTask} from "@/app/lib/quant-research-server";
export async function POST(request:Request){try{const body=await request.json() as {goal?:string;task_id?:string};const taskId=body.task_id??(await saveNewQuantTask(body.goal??"")).id;const preview=await previewQuantWorkspace(taskId);return NextResponse.json({type:"quant_workspace_preview",task_id:taskId,extraction:preview.task.extraction,patch:preview.patch,requires_confirmation:true});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法生成预览"},{status:422});}}
