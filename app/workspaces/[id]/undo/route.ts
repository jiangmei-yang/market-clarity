import {NextResponse} from "next/server";import {undoDashboard} from "@/app/lib/dashboard-server";export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const body=await request.json() as {confirmed?:boolean};if(body.confirmed!==true)return NextResponse.json({message:"请明确确认撤销"},{status:422});return NextResponse.json({workspace:await undoDashboard((await params).id)});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法撤销"},{status:409});}}
