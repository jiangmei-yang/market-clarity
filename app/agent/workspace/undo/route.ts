import {NextResponse} from "next/server";import {undoDashboard} from "@/app/lib/dashboard-server";export async function POST(request:Request){try{const body=await request.json() as {workspace_id?:string;confirmed?:boolean};if(!body.workspace_id||body.confirmed!==true)return NextResponse.json({message:"请明确确认撤销"},{status:422});return NextResponse.json({workspace:await undoDashboard(body.workspace_id)});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法撤销"},{status:409});}}
