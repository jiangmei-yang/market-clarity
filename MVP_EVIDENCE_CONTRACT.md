# AI 策略研究室 MVP Evidence Contract

> 版本：2026-07-31.2
> 当前判断：**工程纵向切片可运行；市场层 MVP 尚未由外部行为验证。**

本文件是 Module 7/8 口径下的单一证据入口。旧完成报告只代表工程里程碑，不能证明需求、留存或商业可行性。

## 1. Assumption

目标用户是不懂量化、但希望用历史数据检验一个投资想法或稳定性目标的普通 A 股投资者。

可证伪假设：目标用户能用一句大白话启动研究，确认系统整理的规则，看懂统一比较为什么保留或淘汰候选，并把一个结果保存为以后可重检的研究方法，同时不把历史检验误解为荐股或未来预测。

## 2. User behaviour

唯一主指标：**外部新手独立保存研究方法率**。

有效完成必须包含：开始任务、生成并查看规则、运行统一比较、选择一个单独策略、主动保存。只打开页面、口头称赞或只跑到结果页不计入。

- 首轮：5/5 位外部新手无引导完成；
- 扩展：三类目标用户各 5 位，总完成率 ≥80%；
- ≥80% 在保存后三道客观理解题全部答对；首轮访谈另做无提示复述；
- 至少 40% 在 14 天后实际再次打开并重检，不用态度题替代；
- 团队成员、Codex 和代理新手不计入外部证据。

## 3. Experiment

1. 访谈 5–10 位目标用户最近一次尝试验证投资想法的经历，不先展示产品；
2. 用 `/pilot/strategy-lab` 对 5 位设计伙伴执行同一无引导任务；
3. 修复重复障碍，再扩到三类用户各 5 位；
4. 7–14 天后观察是否自然回来重检或复制；
5. 用 `/evaluation` 查看化名化漏斗；三道客观题自动计分，无提示复述另由两位人员独立编码。

## 4. Vertical slice

```text
想法或目标
→ 确认股票范围、规则、成本和比较标准
→ 受控候选与传统参照统一三段检验
→ 查看单个策略及保留/淘汰原因
→ 保存为可重检、可复制的研究方法
```

交易前决策审查是保存后的下游场景，不进入本轮主指标分母。

## 5. Evaluation

| 类型 | 预设门槛 | 当前证据 |
|---|---|---|
| 主指标 | 首轮 5/5；扩展至少 15 人且 ≥80% 保存 | 0 位外部用户，未验证 |
| 理解质量 | ≥80% 保存后三题全对；无提示复述另作质性访谈 | 0 位外部用户，未验证 |
| 流程可靠性 | 中位完成时间 ≤5 分钟；放弃、失败与重试分别报告 | 匿名事件路径已实现 |
| 成本与风险 | P95 运行 ≤10 秒；0 自动交易；0 P0 安全事故 | 自动交易能力不存在；性能待真实样本；负责人必须为当前构建和批次提交安全复核，复核只覆盖提交前的事件，新增事件会自动使该门槛失效；未复核按不通过处理 |

## 6. Release boundary

- 受众：登录后的受邀体验者，按团队排错 → 5 位设计伙伴 → 15 人邀请测试扩大；
- 任务：仅研究受控低频策略，不荐股、不预测、不连接券商；
- 数据：使用服务端 HMAC 化名研究 ID；行为事件不保存输入正文、股票清单、金额或直接身份；应用统计、读取和导出只覆盖最近 90 天，每次访问触发清理，休眠数据库的物理擦除可能延迟，参与者可提前删除本人记录；
- 控制：用户确认规则、选择单个策略并主动保存；
- 恢复：模型不可用时回退白名单规则；数据或计算失败时明确停止并允许重试；
- 停止：只有 `MVP_PILOT_ENABLED=true` 才接收新事件；漏配或 `false` 均停止写入，保留本人读取、导出和删除；
- 负责人：产品负责人负责招募、用独立复核码登记当前批次 P0 事故数，以及 Persist/Narrow/Pivot/Stop 决策；未登记不是“零事故”。

构建与批次冻结：一个体验码首次写入后绑定唯一服务端构建号与样本关系；部署新构建必须签发新的外部体验码。评估页只读取当前环境明确配置的外部体验码和构建号，不按“最后一条事件”自动切换。

保留边界：若生产部署要求在第 90 天准时完成底层擦除，还必须配置独立的月度清理任务；当前应用不伪称休眠数据库已经准时物理删除。

## 7. Decision rule

| 结果 | 决定 |
|---|---|
| 主指标和全部护栏通过 | **Persist**：进入 20 人邀请测试，不扩大策略自由度或交易权限 |
| 仅一个入口或分群通过 | **Narrow**：只保留该入口或分群并复测 |
| 能跑完但看不懂、没有保存或自然复访 | **Pivot**：重做任务和价值呈现，不增加因子 |
| 15 人后无分群达到 60%，或出现不可接受风险 | **Stop**：停止方向并保存失败证据 |

## 当前仍需真人完成的证据

- 5–10 次问题访谈；
- 首轮 5 位、扩展 15 位外部新手的无引导行为；
- 两位人工编码者对理解复述的判断；
- 14 天自然复访；
- 真实运行时延和失败率。

在这些数据出现前，正确表述是“工程上已具备 MVP 实验条件”，不是“MVP 已验证”。
