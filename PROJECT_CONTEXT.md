# 项目上下文

> 最后更新：2026-07-29
>
> 完整团队交接材料见 [PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)。

## 项目目标

安心看股（Market Clarity）是面向 A 股普通投资者的网页版研究与交易前决策验证工作台。用户通常已经拥有行情、新闻和社交信息，本项目帮助他们在行动前分清事实与推断、核实关键信息、计算个人金额影响，并对照自己预先确认的规则作决定。

成功不是预测更准或鼓励更多交易，而是让用户更快发现遗漏、理解风险，并留下可以复盘的原始理由和最终选择。

## 核心产品承诺

```text
研究公开信息
→ 并列阅读支持、反方与待核实证据
→ 写下股票、操作、金额、理由和失效条件
→ 确认理由拆解
→ 核实外部说法
→ 确定性计算仓位与损失情景
→ 对照个人规则
→ 用户维持、修改或延后
→ 保存证据快照和决定
```

## 非目标

- 荐股、目标价、确定性涨跌预测或收益承诺；
- 自动交易或券商连接；
- 全市场高频回测和生产级选股平台；
- 用 AI 替代金额、风险或统计计算；
- 把未核实、二手或过期资料包装成事实；
- 当前阶段正式交付小程序、Android、iOS 或完整 PWA。

## 架构

```text
Information Layer
行情 / 财务 / 公告 / 新闻 / 用户提供资料
                 ↓
Decision Layer
理由拆解 / 信息核实 / 个人规则 / 金额影响 / 用户选择 / 记录复盘
```

Decision Layer 是核心差异化。Information Layer 可替换供应方，但只能提供证据，不能替用户决定。

### 代码实现线

- `web/`：正式桌面网页平台。新增桌面体验、网页 API route 和评估页工作优先从这里开始。
- `app.py`、`pages/`、`static/`：较早的 Streamlit/FastAPI MVP、演示和独立工具页面。
- `src/`：跨界面复用的 Python 业务逻辑。
- `api.py`：最小 FastAPI 后端入口。

两套界面代码并存是当前事实，不代表同一功能需要维护两份。开始任务前必须确认目标交付面。

## 核心模块

| 模块 | 主要路径 | 说明 |
|---|---|---|
| 决策验证 | `src/decision_review/`、`web/app/api/evidence/` | 理由、说法、证据、核实状态和审查编排 |
| 风险计算 | `src/risk_engine/` | 仓位、集中度、情景损失和个人规则 |
| 定量核实 | `web/app/lib/quant-verification.ts`、`web/app/lib/quant-research.ts` | 用户确认后运行的历史规则检验 |
| 数据服务 | `src/data_providers/`、`src/services/` | 行情、公告、新闻、财务和降级 |
| 正式 Web | `web/app/` | 工作台、研究、决策、评估和设置 |
| 评估 | `web/app/lib/course-evaluation.ts`、`web/tests/` | 规则案例、模型评估与幻觉防护 |

## 数据与 AI 原则

- 默认使用真实公开数据；必要时回退缓存或明确标记的演示数据。
- 数据状态必须显示更新时间、来源、覆盖范围和可靠性。
- AI 可以拆解语言和整理有限证据，但不能创造事实。
- 所有 AI 肯定结论必须引用已取得的证据。
- 模型失败时，核心流程应回退到规则模式或显示资料不可用。

## 当前关键限制

- 公告核实仍大量依赖标题关键词，需要发展为原子说法与正文/片段证据关系。
- “部分支持”“明确冲突”和跨层统一状态语义仍需完善。
- 上游公开数据可能限流、超时或字段改变。
- 真实外部用户的完成率、复访和付费行为证据仍需积累。
- 多人并行协作时必须使用小范围提交和明确所有权，不得覆盖其他功能分支内容。

## 当前优先级

1. 完善 Decision Validation 的可追溯证据链。
2. 保持确定性风险计算、失败降级和安全边界可靠。
3. 运行真实用户的无引导决策任务，验证理解与复访。
4. 冻结跨平台扩张，减少重复界面和无证据功能堆叠。

## 关键文档

- `PROJECT_HANDOFF.md`：完整项目与协作说明；
- `TASKS.md`：当前负责人、范围与验收；
- `DECISION_VALIDATION_HANDOFF.md`：决策验证专项方案；
- `README.md`、`web/README.md`：产品定位、正式入口与当前能力；
- `MVP_SCOPE.md`：冻结范围；
- `CORE_USER_FLOW.md`：核心流程；
- `DECISION_LAYER_ARCHITECTURE.md`：Decision Layer 架构；
- `EVALUATION.md`：产品指标与反指标；
- `docs/decisions/`：已确认的重要决策记录。
