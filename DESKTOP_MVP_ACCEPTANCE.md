# 安心看股桌面网页版 MVP 验收

验收日期：2026-07-22
范围：桌面网页版；不包含部署、手机端、小程序、原生 App 和真实用户证据。

## 结论

当前版本达到 **91/100**，可以进入 5–10 位目标用户的受控测试，也可以用于创业课课堂演示。

这不是“所有设想都做完”的评分，而是按 `MVP_90_POINT_PRODUCT_PROMPT.md` 对当前可运行产品逐项取证。正式模式不依赖演示价格、演示公告或 AI Key 才能完成核心流程。

## 评分

| 维度 | 得分 | 证据 | 仍可提高 |
|---|---:|---|---|
| 核心价值清晰度 | 11/12 | 工作台首屏依次显示真实市场、现在先处理、与持仓有关的变化和组合暴露；研究与决策不是功能卡片入口 | 有持仓用户的“今日变化”排序仍需真实用户验证 |
| 真实可用性 | 16/18 | 任意有效 A 股代码、持仓 CSV、ETF 公共披露和交易 CSV 可运行；无效代码不再误用指数；正式模式排除演示数据 | 免费公开源仍可能限流；同行数据覆盖有限 |
| 决策闭环 | 16/16 | 研究问题和证据进入审查，理由可修正，金额实时计算，用户可维持/修改/延迟，历史可重新核实 | — |
| 数据可信度 | 13/14 | 行情、公告、财报、ETF 均显示来源、时间、口径、部分/缓存/不可用；价格图只用真实历史数据 | 部分财务字段和行业映射受公开源覆盖限制 |
| UI/UX | 12/14 | 无营销 Hero、无卡片墙；研究默认显示价格事件图；正文不低于 12px；桌面无明显横向溢出 | 尚需在更多 Windows 字体缩放设置下人工检查 |
| AI 技术体现 | 9/10 | 自然语言结构化、有限证据检索、引用、无 Key 规则降级、提示注入隔离、输出合规/引用校验 | 尚未在 UI 中显示提示词版本和证据 ID |
| 创新与差异化 | 8/8 | 真实信息与个人持仓、原判断、个人规则和计划金额连成一条可复核记录，不是另一份荐股报告 | — |
| 商业验证准备 | 4/5 | 匿名编号、完成耗时、计划变化、风险理解、满意度、复用意愿、付费测试意愿、困惑步骤可导出 | 需要真实用户样本，不应由模拟结果代替 |
| 稳定性与合规 | 2/3 | 前后端测试通过；无密钥入前端；无自动交易、收益承诺和模型金额计算 | 公开数据源无 SLA，生产监控尚未纳入本轮 |
| **总分** | **91/100** |  |  |

## 已通过的核心任务

### 工作台

- 真实返回上证指数、沪深300、创业板指，显示市场状态、来源与读取时间；
- 无持仓时不显示样例组合，直接解释导入后得到什么；
- 有持仓时按股票读取行情和法定披露，并对照最近判断与个人边界；
- 失败时显示重试、部分可用或不可用，不用样例事件补齐。

### 股票研究

- 有效 A 股名称和 6 位代码搜索；无效代码被证券名单拦截；
- 默认打开价格与事件图，不先展示长段 AI 摘要；
- 1 月、3 月、1 年分别使用 22、66、250 个真实交易日；
- 事件、原文、成交量和持仓金额影响在同一研究路径中；
- 资料不存在时明确“未找到不等于事实不存在”。

### 决策验证

- 从研究页带入股票、待核实问题和证据快照；
- 理由拆成事实、外部说法、个人推断、紧迫性和来源，用户可逐项修改；
- 仓位、规则差值和 10%/20%/30% 下跌金额由确定性代码计算；
- 用户必须自行选择维持、修改或稍后再看；
- 原金额、最终金额、规则快照、证据快照、耗时和复核日期均保留。

### 持仓、ETF 与交易复盘

- 持仓支持手工、CSV 导入、预览、导出和恢复，所有核心页面共用同一份本地数据；
- ETF 诊断使用真实定期披露，显示披露日期、过期天数、底层持仓和重复覆盖边界；
- 交易复盘完成 CSV 解析、FIFO、手续费、未平仓、无法匹配卖出、集中度规则和时间线；
- 无当前市价时不计算未实现盈亏。

### AI 与数据安全

- 没有 OpenAI Key 时使用规则和关键词解析，核心功能仍可运行；
- LLM 不计算金额、仓位、FIFO 或规则阈值；
- 外部网页文本被标记为不可信输入；
- 荐股语言、收益承诺、无效引用和无引用肯定结论自动降级为规则结果；
- 正式模式过滤后端演示价格和演示证据；
- 密钥只从服务器环境变量读取。

## 自动化与浏览器证据

- Python：`PYTHONPATH=. .venv/bin/pytest -q`，108 项全部通过；
- 前端：`npm run lint` 通过；
- 前端生产构建与测试：`npm test`，4 项全部通过；
- 构建路由包含工作台、ETF、交易复盘及 8 个数据 API；
- CSS 回归测试禁止 8–11px 正文，并验证 1160px 窄桌面断点；
- 浏览器实测工作台、股票研究、决策输入、ETF 和交易复盘无横向溢出。

## 进入用户测试后的首要问题

1. 用户是否会在下单前主动完成这一步，而不是觉得麻烦；
2. “价格与事件”是否帮助用户区分时间相关与因果关系；
3. 哪一项最能改变计划：仓位差值、金额损失、证据不足还是失效条件；
4. 用户第二次是否愿意回来查看到期判断和持仓变化；
5. 用户愿意为什么付费：持续持仓变化、批量复核，还是更稳定的数据覆盖。

这些问题必须由真实用户回答，不能继续通过增加页面或模拟评分替代。
