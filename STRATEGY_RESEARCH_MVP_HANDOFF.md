# AI 策略研究室 MVP 交接

> 完成日期：2026-07-30
> 正式页面：`/quant/factors`（新版桌面网页）

## 已实现

- 两种新手入口：有模糊想法、只有研究目标；“只有目标”会先给出低波动、趋势加低波动、动量加风险过滤三个可解释起点，用户选定后才确认规则。
- `strategy-dsl-v1`、因子/过滤/频率/成本 allowlist、未知字段与越界拒绝、confirmation hash 失效机制。
- 同股票样本等权、传统 60 日动量、20 日低波动、5 日反转对照，以及从白名单参数网格批量扫描的 50–2000 个去重候选；最多 6 个验证胜出者统一进入最后检查。
- 确定性横截面引擎：逐期有效股票、非重叠持有期、成本、换手、净值、回撤、波动、Sharpe、阶段稳定性、集中度和敏感性。
- 50% 研究、25% 验证、25% 锁定测试；模型不读取价格矩阵和锁定结果，最多 6 个候选进入锁定测试。
- 确定性结论状态、候选漏斗、逐方法保留/淘汰原因、统一比较表、收益/回撤权衡、成本影响、稳定性、数据审计与文字替代。
- 用户可在原始策略、传统研究副本和通过门槛的有限候选中明确选择要保存的方法；不会自动替用户选择第一个幸存候选。
- D1 用户快照保存紧凑运行与策略；只持久化原方法、固定参照、最终检查候选和跨三段均匀抽样的曲线点，旧长指纹与旧运行会在下一次写入时迁移压缩；支持查看规则/限制/父版本、复制后回到白话入口继续修改、2–3 项按收益/回撤/换手/稳定性比较、按原规则重检、确认删除，不保存完整价格矩阵。
- 保存的方法可作为 `strategyResearchEvidence` 历史证据带入决策验证页面；交接对象带策略/版本/数据截止/限制，仍保持 `research_only: true`、`allow_live_order: false`。
- 认证、请求大小、股票/候选预算和 `allow_live_order: false` API 守卫。

## AI 实际负责什么

若用户配置了可用且具备量化规则能力的模型，模型只能返回白名单 `factor_ids`、`research_goal` 和双周/月度频率。输出再经本地规划器与 DSL 校验；失败或无模型时自动使用规则模式。模型不计算数字、不选择当前股票、不接触价格矩阵或锁定测试结果。

候选的组合、生成理由、校验、回测、淘汰、目标比较和白话结论均由确定性代码完成。保存卡片使用所选方法自身的状态与原因，不复用整次运行的总标题。

## 数据来源与限制

当前默认数据为 24 只股票、1,000 个交易日的固定合成演示快照，状态始终为 `demo`，截止 2025-12-31。它仅用于验证产品闭环，不是真实、缓存或实时行情，不可用于当前个股判断。

当前样本不是历史时点成分股、未包含退市股票，存在幸存者偏差。已建模日线收盘、低频等权、简化佣金/印花税/滑点；未建模真实停牌成交失败、涨跌停排队、冲击成本、复权事件和历史成分股变化。生产化前最重要的数据任务是接入带来源、复权、可靠性与历史时点股票池的长周期批量适配器。

## “更符合目标”的判断

候选先通过样本期数、逐期有效股票、未来函数、验证区成本后结果、正向时期比例和敏感性门槛，再按用户在研究前确认的目标比较回撤、稳定性、换手或成本后表现。没有 AI 综合分，也不使用“最佳策略”“验证成功”或建议采用等文案。

## 防过拟合

- 用户在打开锁定区前预先确认 50–2000 组扫描预算，候选去重后才运行；
- 严格按时间切成三段；
- 选候选时锁定测试为 `null`，不会传给 AI 或排序器；
- 只让最多 6 个验证胜出候选进入锁定测试；
- 锁定后相同条件只幂等重开上次结果，不重算、不继续调参；实质修改必须生成新的 confirmation hash；
- 累计尝试达到 3 次时提示历史挑选风险；
- 保存数据、引擎和方法版本，便于复现。

## 验证证据

- Node 24 工作区运行时生产构建通过；176 个路由被识别。
- 完整新版网页测试 179/179 通过；本功能相关测试 38/38 通过；修改文件 ESLint 通过；Node 24 生产构建通过。
- 本地浏览器已完成“只有目标 → 三起点切换 → 确认 → 运行 → 统一比较 → 用户选择传统副本保存 → 两项已保存方法比较 → 原规则重检 → 带入决策验证”的完整闭环。
- 截图：`output/strategy-research-1440.png`、`output/strategy-research-results-1080.png`。

## 2026-07-30 新手体验迭代

- 首屏改成单任务入口：两种开始方式、三条可点示例、一句话输入和一个主按钮；首屏可见文字由 283 字降到 111 字。
- 确认页把规则改成 6 步“策略配方”，自动准备的数据、传统对照与候选上限做成三张状态卡，参数和说明默认收起。
- 结果页新增可点击的 5 段候选漏斗、研究/验证/锁定时间轴、已计算净值的播放/暂停、成本前后条形图、回撤比较和阶段稳定性图；播放明确不是实时预测。
- 结果首屏先显示生成数、锁定数、保留数和一句白话结论；专业表格、证据解释、数据限制与已保存方法管理按需展开。
- 运行阶段采用真实不确定进度，不显示伪造的百分比；月度原策略不再生成一个完全相同的“低频候选”。
- 窄屏验收无横向溢出，核心筛选按钮为 44px；键盘焦点可见，并提供 `prefers-reduced-motion` 降级。

此前内部启发式分数对应的旧结果页已被直接使用反馈推翻，不再作为当前版本结论。当前版本的真实外部新手评分仍待第 5 人协议执行。

全仓 ESLint 仍被本任务范围外的 effect 规则与 dashboard 命名等 15 个既有错误阻断；本轮修改文件的定向 ESLint、Node 24 生产构建和完整 179 项测试均通过。

## 2026-07-30 最终候选工作区与存储复验

- 结果默认先给一句结论和 `扫描 → 最后检查 → 最终保留` 三段数字，再把全部 6 个最终检查候选放在左侧、所选候选与最佳传统参照的图放在右侧；不再需要先翻过长文或切换标签。
- 每个候选单独显示成本后收益、最大回撤、参照值和最终状态；默认选择第一个真正通过最终检查的候选，不再让失败项成为页面第一印象。
- 图表补齐净值纵轴、日期横轴、研究/验证/锁定区、阶段边界、实线/虚线和播放进度；曲线提前结束明确解释为未进入下一阶段，而不是计算中断或后续为零。
- 批量网格候选用因子权重、调仓节奏、过滤条件和入选比例命名；中英文均能区分规则相近的候选。
- 真实账号历史较多时曾触发快照上限，现已通过仅压缩策略研究自身的旧运行、曲线和候选指纹修复；500 组研究可直接完成，不要求用户删除其他产品数据。
- 中文桌面、英文、窄屏、封存结果重开、500 组运行和存储迁移均已浏览器复验；页面无横向溢出，临时窄屏视口已恢复。

## 2026-07-30 低文字连续流程复验

- 基础路径严格收敛为三个主要动作：说出想法或目标、查看四项规则、点击“确认并开始比较”；删除额外确认复选框。
- 确认页默认只展示股票范围、历史特征、选择方式和调整节奏，股票搜索、轮数、费用和完整规则按需展开。
- 结果页删除默认四标签导航，改为审慎结论、费用/回撤关键数字、五步候选流转和三条历史曲线的连续顺序。
- 曲线图例直接写明“你的原方法 / 固定参照 / 第二轮未通过 / 保留”，避免只看收益数字误解被淘汰候选。
- 固定演示数据在入口、确认、运行和结果持续标记；返回修改条件会清除上一轮保存提示。
- 浏览器已复验“有想法”和“只有目标”两条路径、有候选和无候选两种结果、保存成功反馈、1080/1440 无页面横向溢出和键盘焦点可见。
- 英文开关不再只翻译外壳：入口、三步导航、目标选项、起点、四项规则、运行状态、结果漏斗、历史曲线、单方法状态和保存复用主路径均有英文界面；英文 drawdown、momentum、volatility、biweekly 等输入也进入同一白名单规划器。
- 2026-07-30 的无引导浏览器复验实际完成英文目标路径；结果首屏为结论、3 个关键数字、5 步漏斗和历史曲线，16 个方法均可单独展开并查看 3 段指标。
- 英文高级设置也已实际展开复验：可在自动/手动间切换，查看四个预设，进入名称/代码搜索自选，调整最多持仓、每轮候选和轮数；专业数据层的成本、稳定性、逐方法数值与数据限制均保持英文且无横向溢出。
- 结果数量现已严格对应：所有保留候选都有独立名称、保留理由、成本后结果、最大回撤和稳定性卡片，并全部默认加入曲线；原方法和固定参照使用不同角色标签，不再把第一个保留候选的指标当作整次运行汇总。
- 漏斗下直接解释为什么提前停止；图例移到曲线之前，并逐条写明“展示完整历史 / 保留且完成最后检查 / 停在验证区”等状态。

## 尚未完成的外部证据

尚未招募 5 名无量化背景的真实外部用户，因此附件要求中的 2 分钟草案率、5 分钟完成率、结果理解率和复访率没有真实用户证据。浏览器验收和自动测试不等同于用户研究。下一轮最值得验证的假设是：用户能否在不打开专业详情时，准确复述“传统对照、锁定测试、成本变化和至少一个数据限制”。

真实新手的参与者条件、双入口任务、逐人记录表、95 分固定算法和缺陷分级已建立在 `STRATEGY_RESEARCH_NOVICE_VALIDATION.md`。当前明确记录为外部真实参与者 0/5；没有匿名证据链接的“通过”不得计入验收。

## 独立审查结论

- Builder 完成实现与真实浏览器闭环；Critic 经四轮反例攻击后确认 P0=0、P1=0，六维 PASS。
- 独立 Evaluator 按最初产品定义给出 PASS，内部工程验收 96/100：需求 20/20、逻辑 20/20、边界 16/16、代码质量 13/15、测试 14/15、实际运行 13/14。
- 该 96 分仅是可复核的内部工程评分，不是外部真实新手评分。真实用户 95 分仍须由 UV-01 的 5 人无引导测试产生。

## 2026-07-31 课堂展示视觉版

- 新增 `/quant` 独立产品首页，以一句话价值、产品图表示意、三步闭环和防过拟合说明建立公司级产品入口；
- `/quant/factors` 改为 Strategy Lab 专用导航，隐藏与当前任务无关的全局悬浮工具；
- 结果页默认先显示白话结论、筛选漏斗、全部最终候选和所选候选的带坐标回测图，方法学和专业数据移到图后折叠层；
- 视觉方向借鉴 TradingView 的深色图表画布与 Composer 的单句价值主张，但保持 research-only、不荐股、不下单、不承诺未来收益；
- 最新验证：178 路由生产构建通过，前端全量 197/197 通过；浏览器完成 500 组扫描、单候选切换与动态回测播放。Critic 与 Evaluator 最终均确认 P0=0、P1=0，工程展示完成度 98/100；外部用户证据仍为 0/15，不将本次工程验收包装为市场验证。

## 2026-07-31 Composer 式创建流程

- 入口升级为统一研究指令栏：一句自然语言即可开始，并以旅程按钮区分“已有模糊想法”和“只有改善目标”；
- 草案升级为对话式确认和四段策略逻辑画布，新手先看到系统听懂的规则，再决定是否运行；
- 100/500/1000 扫描规模与“达到目标即检查 / 跑满预算”提供一键选择，高级滑杆、数字输入和股票样本仍保留在展开层；
- 修复跨研究政策缓存误用：500 组提前停止的封存结果不再冒充 1000 组完整预算结果；只有相同搜索政策才会幂等复用；
- 浏览器复验的 1000 组完整运行得到 2 个最后检查候选、0 个最终保留，页面诚实列出每个失败候选和固定传统参照，不取平均值、不伪造通过；
- 最新证据：策略专项 49/49、前端全量 199/199、生产构建、定向 ESLint 与差异检查通过。以上仍是内部工程证据，不替代外部新手行为验证。

## 2026-07-31 全平台首页与导航整合

- `/` 现为 Market Clarity 全平台首页，浅色蓝色视觉下同时介绍 AI 工作台、策略研究室和研究证据；“打开 AI 工作台”进入 `/workspace`，“进入策略研究室”进入 `/quant/factors`；
- `/pricing` 为课程 MVP 的诚实定价说明：免费研究体验与 ¥19/月研究版候补实验并列，当前不接支付、不会扣费，也没有自动交易或券商连接；
- `/quant`、`/quant/factors` 的品牌入口返回全平台首页，导航统一加入 AI 工作台、策略研究室和定价，不再形成无法返回上层的孤岛；
- 动效使用 GSAP ScrollTrigger 和 reduced-motion 降级，服务于产品流程的渐入、固定叙事和界面缩放，不模拟回测进度、不改变研究数字；
- 设计只借鉴 Composer 的交互节奏与信息架构，未复制其品牌、文案、客户证明、社区内容或交易执行能力。

本轮浏览器验证已完成首页、工作台、策略研究产品页、策略研究操作页和定价页的真实导航；策略入口可进入双模式并生成三个受控起点。完整测试与独立 Critic/Evaluator 复核结果将在本轮结束后补记。
