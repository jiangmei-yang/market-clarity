# 当前任务与负责人

> 状态日期：2026-07-29
>
> 状态值：`BACKLOG` / `READY` / `IN PROGRESS` / `BLOCKED` / `REVIEW` / `DONE`

本文件记录团队当前工作所有权。负责人开始任务时应更新状态、分支和实际范围；完成后填写测试证据和剩余限制。

## 当前任务板

| ID | 工作包 | 负责人 | 状态 | 主要交付 | 依赖/备注 |
|---|---|---|---|---|---|
| DV-01 | Decision Validation 基线审计与 schema | Wenyuan Mi（待团队确认） | READY | 当前数据流、Claim/Evidence/VerificationResult schema、兼容方案 | 先读专项 handoff；修改公共 schema 前通知相关负责人 |
| DV-02 | 原子说法拆解与用户确认 | Wenyuan Mi（待团队确认） | BACKLOG | 混合理由拆解、金额/日期/主体抽取、规则降级 | 依赖 DV-01 |
| DV-03 | 正式资料与证据关系 | Wenyuan Mi（待团队确认） | BACKLOG | 来源等级、正文/片段关系、部分支持与冲突状态 | 依赖 DV-01；标题命中不能直接算支持 |
| DV-04 | 决策影响说明与前端状态 | Wenyuan Mi（待团队确认） | BACKLOG | 已确认/未确认/判断影响/下一步问题及失败状态 | 依赖 DV-02、DV-03；避免直接扩写大页面 |
| DV-05 | 专项评估与红队 | Wenyuan Mi（待团队确认） | BACKLOG | 至少 30 个案例、提示注入和来源失败测试 | 可与 DV-02/DV-03 并行补案例 |
| UV-01 | 外部用户无引导测试 | 待分配 | READY | 3–5 人理解测试、完成率、关键原话和复访行为 | 核心验证流程稳定后执行 |
| REL-01 | 公开数据源可靠性复核 | 待分配 | BACKLOG | 超时、缓存、过期和不可用状态证据 | 不把单次网络结果写成长期结论 |
| DOC-01 | 团队协作文档基线 | 项目负责人 | REVIEW | `AGENTS.md`、`PROJECT_CONTEXT.md`、`TASKS.md`、ADR 目录 | 本轮新建，需团队确认负责人名称和任务优先级 |

## Decision Validation 交付顺序

```text
DV-01 schema
  ├─→ DV-02 说法拆解
  └─→ DV-03 证据关系
          ↓
       DV-04 UI 与决策影响
          ↓
       DV-05 完整红队与验收
```

详细要求见 `DECISION_VALIDATION_HANDOFF.md`。

## DV-01 验收标准

- 画出“用户理由 → 原子说法 → 证据 → 核实状态 → 决策影响 → 用户选择”的当前数据流；
- 定义 Python/TypeScript 一致的 schema；
- 统一 `supported`、`partially_supported`、`contradicted`、`insufficient`、`unavailable`、`stale`；
- 给出旧字段兼容和决策记录迁移说明；
- 相关单元测试通过。

## DV-02 验收标准

- 混合理由可拆成多条独立说法；
- 事实、外部事件、因果推断、市场预测和个人动机不混为一类；
- 用户可修改并确认拆解；
- 无 AI Key 或模型失败时保留规则模式；
- 确认拆解不被表达为确认事实。

## DV-03 验收标准

- 正式披露优先于媒体、观点和用户转述；
- 标题关键词只用于候选召回；
- 记录来源、日期、链接、片段和覆盖范围；
- 可表达只支持事件但不支持金额/时间；
- 未找到不等于证伪；不可用/过期时不产生新的实时结论。

## DV-04 验收标准

- 每条说法独立显示状态；
- 固定显示“已确认、未确认、对原判断的影响、下一步问题”；
- 用户能区分证据不足、明确冲突和数据源失败；
- 不显示综合买卖评分，不预选维持/修改/延后；
- 决策记录保存当时的证据快照。

## DV-05 验收标准

- 至少 30 个可重复运行的专项案例；
- 覆盖完整支持、部分支持、冲突、无结果、过期、不可用、错误主体和日期；
- 覆盖提示注入、伪造引用、确定性买卖要求和二手来源升级；
- 所有肯定结论引用合法 evidence ID；
- 现有 Python 与新版网页相关测试无回归。

## 开始任务时填写

```text
任务 ID：
负责人：
分支：
开始日期：
预计完成：
本次范围：
明确不做：
可能冲突文件：
```

## 完成任务时填写

```text
任务 ID：
提交/PR：
改动摘要：
测试命令与结果：
截图或样例：
未解决限制：
需要下一位负责人处理：
```
